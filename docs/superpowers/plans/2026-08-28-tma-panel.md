# Мини-апп как панель управления — план реализации

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Мини-апп выглядит как мобильный веб Linkeon, показывает интерфейс на языке пользователя, переключает реального собеседника в боте и не выглядит недоделанным.

**Architecture:** Экраны остаются своими (отдельный лёгкий бандл), но визуальный язык переезжает в общие презентационные компоненты `src/shared/ui/`, которыми пользуются оба бандла. Тяжёлые веб-компоненты не импортируем.

**Tech Stack:** React 18, TypeScript, Vite, Tailwind, i18next, Vitest.

**Спек:** `docs/superpowers/specs/2026-08-28-assistants-in-bot-and-tma-panel-design.md`
**Зависимость:** Task 4 имеет смысл только после части 1 (`feat/tg-assistants-in-bot`) — до неё переключение ассистента ни на что не влияет.

---

## Структура файлов

| Файл | Ответственность |
|---|---|
| `src/tma/tmaLanguage.ts` (создать) | Чистая функция выбора языка из трёх источников |
| `src/tma/telegram.ts` (правка) | Отдать `user.language_code` из initData |
| `src/tma/boot.ts` (правка) | Применить язык после входа |
| `src/utils/downscaleImage.ts` (создать) | Уменьшение картинки перед загрузкой, общее для веба и мини-аппа |
| `src/shared/ui/` (создать) | `Card`, `Avatar`, `BalanceBadge`, `BottomNav` — общий визуальный язык |
| `src/tma/screens/*.tsx` (правка) | Переверстка на общие компоненты |
| `src/tma/screens/assistantsFlow.ts` (правка) | Подтверждение вместо молчаливого закрытия |

---

## Task 1: Язык интерфейса берётся от пользователя, а не от устройства

**Files:**
- Create: `src/tma/tmaLanguage.ts`, `src/tma/tmaLanguage.test.ts`
- Modify: `src/tma/telegram.ts`, `src/tma/boot.ts`

Владелец открыл мини-апп с iPhone и получил английский интерфейс: детектор i18next настроен на `['querystring', 'localStorage', 'navigator']`, а в WebView Telegram localStorage пуст — остаётся язык устройства.

**Порядок приоритета исправлен относительно первой редакции спека:** сначала `profile_data.language`, потом `language_code` из initData, потом язык устройства. В спеке было наоборот, и это ошибка: язык профиля — явный выбор человека в Linkeon, а `language_code` всего лишь язык приложения Telegram. Явный выбор обязан побеждать.

- [ ] **Step 1: Написать падающий тест**

```typescript
// src/tma/tmaLanguage.test.ts
import { describe, it, expect } from 'vitest';
import { resolveTmaLanguage } from './tmaLanguage';

describe('resolveTmaLanguage', () => {
  it('язык профиля побеждает язык Telegram', () => {
    expect(resolveTmaLanguage({ profileLanguage: 'ru', telegramLanguage: 'en' })).toBe('ru');
  });

  it('без профиля берётся язык Telegram', () => {
    expect(resolveTmaLanguage({ profileLanguage: null, telegramLanguage: 'de' })).toBe('de');
  });

  it('без обоих — null, детектор i18next решает сам', () => {
    expect(resolveTmaLanguage({ profileLanguage: null, telegramLanguage: null })).toBeNull();
  });

  it('неподдерживаемый язык отбрасывается в пользу следующего источника', () => {
    expect(resolveTmaLanguage({ profileLanguage: 'kk', telegramLanguage: 'en' })).toBe('en');
  });

  it('региональный код сводится к базовому', () => {
    expect(resolveTmaLanguage({ profileLanguage: null, telegramLanguage: 'pt-BR' })).toBe('pt');
  });
});
```

- [ ] **Step 2: Прогнать, убедиться что падает**

Run: `npx vitest run src/tma/tmaLanguage.test.ts`
Expected: FAIL — `Failed to resolve import './tmaLanguage'`

- [ ] **Step 3: Реализация**

```typescript
// src/tma/tmaLanguage.ts
import { SUPPORTED_LANGUAGES } from '../i18n/languages';

/**
 * Какой язык показывать в Mini App.
 *
 * Порядок: явный выбор человека в профиле → язык его приложения Telegram →
 * null (пусть решает детектор i18next по устройству).
 *
 * Почему профиль первый: profile_data.language человек выставил сам в
 * настройках Linkeon, а language_code — это лишь язык интерфейса Telegram,
 * который на общем устройстве может быть чужим. Первая редакция спека
 * ставила language_code выше — это была ошибка.
 *
 * Неизвестные коды отбрасываем, а не подставляем: i18next на незнакомой
 * локали молча свалится в фолбэк, и мы потеряем следующий источник.
 */
export function resolveTmaLanguage(src: {
  profileLanguage: string | null;
  telegramLanguage: string | null;
}): string | null {
  const supported = new Set(SUPPORTED_LANGUAGES.map((l) => l.code));
  const normalize = (v: string | null): string | null => {
    if (!v) return null;
    const base = v.toLowerCase().split(/[-_]/)[0];
    return supported.has(base) ? base : null;
  };
  return normalize(src.profileLanguage) ?? normalize(src.telegramLanguage);
}
```

Если экспорт в `src/i18n/languages.ts` называется иначе — использовать фактическое имя списка поддерживаемых языков, проверить перед реализацией.

- [ ] **Step 4: Прогнать, убедиться что проходит**

Run: `npx vitest run src/tma/tmaLanguage.test.ts`
Expected: PASS, 5 тестов

- [ ] **Step 5: Отдать language_code из initData**

В `src/tma/telegram.ts` добавить:

```typescript
/**
 * Язык приложения Telegram. initData — обычная query-строка, поле `user` в
 * ней это JSON. Битый JSON здесь не исключение, а норма для чужих клиентов:
 * молча отдаём null, интерфейс просто останется на языке устройства.
 */
export function getTelegramLanguage(): string | null {
  try {
    const raw = new URLSearchParams(getInitData()).get('user');
    if (!raw) return null;
    return JSON.parse(raw)?.language_code ?? null;
  } catch {
    return null;
  }
}
```

- [ ] **Step 6: Применить язык после входа**

В `src/tma/boot.ts`, в `runBoot` после успешного `tmaLogin`:

```typescript
  // Язык применяем ПОСЛЕ входа: profile_data.language доступен только с
  // токеном. До этого момента интерфейс живёт на языке устройства — это
  // видно доли секунды на экране загрузки.
  if (r.status === 'authenticated') {
    try {
      const profile = await deps.getProfile();
      const lang = resolveTmaLanguage({
        profileLanguage: extractProfileLanguage(profile),
        telegramLanguage: deps.getTelegramLanguage(),
      });
      if (lang) await deps.changeLanguage(lang);
    } catch {
      // Язык — не повод не пустить человека внутрь.
    }
  }
```

Добавить в `BootDeps`: `getProfile`, `getTelegramLanguage`, `changeLanguage`; в `defaultDeps` — `() => getJson('/webhook/profile')`, `getTelegramLanguage`, `(l) => i18n.changeLanguage(l)`. `extractProfileLanguage` кладём рядом в `tmaLanguage.ts`: профиль приходит как `[{ profileJson: { profile_data: { language } } }]` — та же форма, что разбирает `extractPreferredAgent` в `assistantsFlow.ts`.

- [ ] **Step 7: Дописать тест на бутстрап**

```typescript
// в src/tma/boot.test.ts
it('после входа применяется язык профиля', async () => {
  const changeLanguage = vi.fn();
  await runBoot({
    ...deps,
    isInsideTelegram: () => true,
    tmaLogin: vi.fn().mockResolvedValue({ status: 'authenticated' }),
    getProfile: vi.fn().mockResolvedValue([{ profileJson: { profile_data: { language: 'ru' } } }]),
    getTelegramLanguage: () => 'en',
    changeLanguage,
  } as any);
  expect(changeLanguage).toHaveBeenCalledWith('ru');
});

it('падение профиля не мешает войти', async () => {
  const state = await runBoot({
    ...deps,
    isInsideTelegram: () => true,
    tmaLogin: vi.fn().mockResolvedValue({ status: 'authenticated' }),
    getProfile: vi.fn().mockRejectedValue(new Error('сеть легла')),
    getTelegramLanguage: () => null,
    changeLanguage: vi.fn(),
  } as any);
  expect(state).toBe('authenticated');
});
```

- [ ] **Step 8: Прогнать оба файла и закоммитить**

Run: `npx vitest run src/tma/`
Expected: PASS, все сьюты каталога

```bash
git add src/tma/tmaLanguage.ts src/tma/tmaLanguage.test.ts src/tma/telegram.ts src/tma/boot.ts src/tma/boot.test.ts
git commit -m "fix(tma): язык интерфейса от пользователя, а не от устройства"
```

---

## Task 2: Аватары перестают весить мегабайты

**Files:**
- Create: `src/utils/downscaleImage.ts`, `src/utils/downscaleImage.test.ts`
- Modify: `src/tma/screens/profileData.ts` (загрузка аватара)

`GET /webhook/avatar` отдал в WebView 3,98 МБ — столько же весит исходник, загруженный пользователем: бэкенд хранит и проксирует файл как есть, уменьшать не умеет (в зависимостях нет ни sharp, ни jimp).

**Что этим решается и что нет.** Уменьшение при загрузке чинит будущие аватары для веба и мини-аппа сразу. **Уже загруженные останутся тяжёлыми**, пока человек не заменит аватар: для них нужна серверная миниатюра — отдельная работа в бэкенде, в этот план она не входит.

- [ ] **Step 1: Написать падающий тест**

```typescript
// src/utils/downscaleImage.test.ts
import { describe, it, expect } from 'vitest';
import { computeTargetSize, MAX_AVATAR_SIDE } from './downscaleImage';

describe('computeTargetSize', () => {
  it('большая картинка ужимается по длинной стороне с сохранением пропорций', () => {
    expect(computeTargetSize(4000, 3000)).toEqual({ width: MAX_AVATAR_SIDE, height: (MAX_AVATAR_SIDE * 3000) / 4000 });
  });

  it('вертикальная ужимается по высоте', () => {
    expect(computeTargetSize(1000, 4000)).toEqual({ width: (MAX_AVATAR_SIDE * 1000) / 4000, height: MAX_AVATAR_SIDE });
  });

  it('маленькая картинка не увеличивается', () => {
    expect(computeTargetSize(200, 150)).toEqual({ width: 200, height: 150 });
  });
});
```

- [ ] **Step 2: Прогнать, убедиться что падает**

Run: `npx vitest run src/utils/downscaleImage.test.ts`
Expected: FAIL — модуль не найден

- [ ] **Step 3: Реализация**

```typescript
// src/utils/downscaleImage.ts

/**
 * Аватар показывается кружком 64×64 CSS-пикселя; 512 с запасом хватает даже
 * на экраны с тройной плотностью. Всё, что больше, — это мегабайты трафика
 * в мобильном WebView ради невидимой разницы.
 */
export const MAX_AVATAR_SIDE = 512;

export function computeTargetSize(width: number, height: number): { width: number; height: number } {
  const longest = Math.max(width, height);
  if (longest <= MAX_AVATAR_SIDE) return { width, height };
  const k = MAX_AVATAR_SIDE / longest;
  return { width: width * k, height: height * k };
}

/**
 * Уменьшает картинку через canvas перед отправкой. Возвращает исходный файл
 * без изменений, если браузер не дал контекст — лучше загрузить тяжёлый
 * аватар, чем не загрузить никакой.
 */
export async function downscaleImage(file: File): Promise<Blob> {
  const bitmap = await createImageBitmap(file);
  const { width, height } = computeTargetSize(bitmap.width, bitmap.height);
  if (width === bitmap.width && height === bitmap.height) return file;

  const canvas = document.createElement('canvas');
  canvas.width = Math.round(width);
  canvas.height = Math.round(height);
  const ctx = canvas.getContext('2d');
  if (!ctx) return file;
  ctx.drawImage(bitmap, 0, 0, canvas.width, canvas.height);

  return new Promise<Blob>((resolve) => {
    canvas.toBlob((blob) => resolve(blob ?? file), 'image/jpeg', 0.85);
  });
}
```

- [ ] **Step 4: Прогнать тест**

Run: `npx vitest run src/utils/downscaleImage.test.ts`
Expected: PASS, 3 теста

- [ ] **Step 5: Применить при загрузке**

В `src/tma/screens/profileData.ts` в функции загрузки аватара заменить помещение файла в `FormData` на:

```typescript
  const body = new FormData();
  body.append('file', await downscaleImage(file), 'avatar.jpg');
  await deps.putForm('/webhook/avatar', body);
```

- [ ] **Step 6: Коммит**

```bash
git add src/utils/downscaleImage.ts src/utils/downscaleImage.test.ts src/tma/screens/profileData.ts
git commit -m "perf(tma): уменьшать аватар перед загрузкой"
```

---

## Task 3: Общий визуальный язык с мобильным вебом

**Files:**
- Create: `src/shared/ui/Card.tsx`, `src/shared/ui/Avatar.tsx`, `src/shared/ui/BalanceBadge.tsx`, `src/shared/ui/BottomNav.tsx`
- Create: `src/shared/ui/Card.test.tsx`
- Modify: `src/tma/App.tsx`, `src/tma/screens/DayScreen.tsx`, `src/tma/screens/AssistantsScreen.tsx`, `src/tma/screens/WalletScreen.tsx`, `src/tma/screens/ProfileScreen.tsx`

Сейчас экраны сверстаны своими классами (`rounded-xl border p-3`), из-за чего мини-апп выглядит самобытно. Выносим то, что реально повторяется, — карточку, аватар, бейдж баланса и нижнюю навигацию.

Веб-компоненты (`ProfileView.tsx` — 957 строк, `TokenPackages.tsx` — 504) не импортируем: они тянут роутер и контексты, ради которых и делался отдельный бандл.

- [ ] **Step 1: Написать падающий тест на карточку**

```tsx
// src/shared/ui/Card.test.tsx
import { describe, it, expect } from 'vitest';
import { render, screen } from '@testing-library/react';
import { Card } from './Card';

describe('Card', () => {
  it('рисует содержимое', () => {
    render(<Card>внутри</Card>);
    expect(screen.getByText('внутри')).toBeDefined();
  });

  it('интерактивная карточка — это кнопка, а не div с onClick', () => {
    render(<Card onClick={() => {}}>жми</Card>);
    expect(screen.getByRole('button')).toBeDefined();
  });
});
```

- [ ] **Step 2: Прогнать, убедиться что падает**

Run: `npx vitest run src/shared/ui/Card.test.tsx`
Expected: FAIL — модуль не найден

- [ ] **Step 3: Реализация карточки**

```tsx
// src/shared/ui/Card.tsx
import type { ReactNode } from 'react';

/**
 * Карточка — основной строительный блок обоих интерфейсов. Классы взяты из
 * мобильного веба, чтобы мини-апп не выглядел чужим приложением.
 *
 * С onClick рендерится <button>, а не <div>: кликабельный div недоступен с
 * клавиатуры и не читается скринридером как действие.
 */
export function Card({
  children,
  onClick,
  disabled,
}: {
  children: ReactNode;
  onClick?: () => void;
  disabled?: boolean;
}) {
  const className =
    'w-full rounded-2xl border border-gray-200 bg-white p-4 text-left shadow-sm ' +
    (onClick ? 'transition active:scale-[0.99] disabled:opacity-50' : '');
  if (!onClick) return <div className={className}>{children}</div>;
  return (
    <button className={className} onClick={onClick} disabled={disabled}>
      {children}
    </button>
  );
}
```

- [ ] **Step 4: Прогнать тест**

Run: `npx vitest run src/shared/ui/Card.test.tsx`
Expected: PASS, 2 теста

- [ ] **Step 5: Остальные три компонента**

`Avatar.tsx` — кружок с картинкой или инициалом на зелёном фоне (как в вебе):

```tsx
export function Avatar({ src, name, size = 40 }: { src?: string | null; name: string; size?: number }) {
  const style = { width: size, height: size };
  if (src) return <img src={src} alt={name} style={style} className="shrink-0 rounded-full object-cover" />;
  return (
    <span
      style={style}
      className="flex shrink-0 items-center justify-center rounded-full bg-green-100 font-semibold text-green-700"
    >
      {name.charAt(0).toUpperCase()}
    </span>
  );
}
```

`BalanceBadge.tsx` — число токенов с разделителями и цветом по порогу из `src/config/balanceThresholds.ts` (переиспользовать существующий `balanceLevel`, не заводить второй набор порогов).

`BottomNav.tsx` — нижняя навигация: принимает список вкладок `{ id, icon, label }`, активную и колбэк. Забирает разметку из текущего `src/tma/App.tsx:31-44` плюс зелёный акцент как в вебе.

- [ ] **Step 6: Переверстать экраны на эти компоненты**

`App.tsx` использует `BottomNav`; `AssistantsScreen` — `Card` + `Avatar`; `WalletScreen` — `Card` + `BalanceBadge`; `DayScreen` и `ProfileScreen` — `Card`.

- [ ] **Step 7: Проверить, что бандл не распух**

Run: `ssh dv@85.192.61.231 'source ~/.nvm/nvm.sh && cd ~/ci/spirits_front && pnpm build 2>&1 | grep tma'`
Expected: `tma-*.js` меньше 50 КБ (до правок было 17 КБ)

- [ ] **Step 8: Коммит**

```bash
git add src/shared/ui src/tma
git commit -m "feat(tma): общий визуальный язык с мобильным вебом"
```

---

## Task 4: Выбор ассистента подтверждается, а не захлопывает окно

**Files:**
- Modify: `src/tma/screens/assistantsFlow.ts:88-91` (`chooseAssistant`), `src/tma/screens/AssistantsScreen.tsx`
- Modify: `src/tma/screens/assistantsFlow.test.ts`

Владелец тапнул по ассистенту дважды: окно закрывалось молча, и ничего не выглядело изменившимся. После части 1 переключение действует на бота по-настоящему — это надо показать.

- [ ] **Step 1: Переписать тест под новое поведение**

```typescript
// src/tma/screens/assistantsFlow.test.ts — заменить сьют про chooseAssistant
describe('chooseAssistant', () => {
  it('переключает и НЕ закрывает приложение', async () => {
    const changeAgent = vi.fn().mockResolvedValue({});
    const closeApp = vi.fn();
    await chooseAssistant('Оля', { changeAgent, closeApp });
    expect(changeAgent).toHaveBeenCalledWith('Оля');
    expect(closeApp).not.toHaveBeenCalled();
  });

  it('ошибка переключения пробрасывается — экран покажет её сам', async () => {
    const changeAgent = vi.fn().mockRejectedValue(new Error('HTTP 500'));
    await expect(chooseAssistant('Оля', { changeAgent, closeApp: vi.fn() })).rejects.toThrow('HTTP 500');
  });
});
```

- [ ] **Step 2: Прогнать, убедиться что падает**

Run: `npx vitest run src/tma/screens/assistantsFlow.test.ts`
Expected: FAIL — `closeApp` вызван

- [ ] **Step 3: Реализация**

В `assistantsFlow.ts` убрать `deps.closeApp()` из `chooseAssistant` и обновить комментарий: закрытие теперь не часть переключения, окно закрывает только явная кнопка «Написать в чат».

В `AssistantsScreen.tsx` после успешного переключения показать состояние: текущим становится выбранный (`setCurrent(name)`), плюс кнопка «Написать» с `closeApp()` — она возвращает человека в переписку с ботом, где уже отвечает новый ассистент.

- [ ] **Step 4: Прогнать тесты**

Run: `npx vitest run src/tma/`
Expected: PASS

- [ ] **Step 5: Коммит**

```bash
git add src/tma/screens/assistantsFlow.ts src/tma/screens/assistantsFlow.test.ts src/tma/screens/AssistantsScreen.tsx
git commit -m "feat(tma): выбор ассистента подтверждается на месте"
```

---

## Task 5: Пополнение баланса из кошелька

**Files:**
- Modify: `src/tma/telegram.ts` (добавить `openLink`), `src/tma/screens/WalletScreen.tsx`
- Modify: `src/tma/telegram.test.ts`

- [ ] **Step 1: Написать падающий тест**

```typescript
// в src/tma/telegram.test.ts
it('openLink открывает ссылку через Telegram, а не window.open', () => {
  const openLinkSpy = vi.fn();
  (window as any).Telegram = { WebApp: { openLink: openLinkSpy } };
  openLink('https://my.linkeon.io/tokens');
  expect(openLinkSpy).toHaveBeenCalledWith('https://my.linkeon.io/tokens');
});

it('вне Telegram деградирует до window.open, а не падает', () => {
  (window as any).Telegram = undefined;
  const openSpy = vi.spyOn(window, 'open').mockImplementation(() => null);
  openLink('https://my.linkeon.io/tokens');
  expect(openSpy).toHaveBeenCalled();
});
```

- [ ] **Step 2: Прогнать, убедиться что падает**

Run: `npx vitest run src/tma/telegram.test.ts`
Expected: FAIL — `openLink is not exported`

- [ ] **Step 3: Реализация**

В `telegram.ts` расширить интерфейс `TelegramWebApp` полем `openLink(url: string): void` и добавить:

```typescript
/**
 * Внешняя ссылка. Через WebApp.openLink она открывается в системном
 * браузере поверх Telegram; window.open внутри WebView в лучшем случае
 * ничего не делает.
 */
export function openLink(url: string): void {
  const app = webApp();
  if (app?.openLink) app.openLink(url);
  else window.open(url, '_blank');
}
```

В `WalletScreen.tsx` добавить кнопку «Пополнить» → `openLink('https://my.linkeon.io/tokens')`.

- [ ] **Step 4: Прогнать тесты**

Run: `npx vitest run src/tma/telegram.test.ts`
Expected: PASS

- [ ] **Step 5: Коммит**

```bash
git add src/tma/telegram.ts src/tma/telegram.test.ts src/tma/screens/WalletScreen.tsx
git commit -m "feat(tma): пополнение баланса из кошелька"
```

---

## Task 6: Полный прогон и выкат

- [ ] **Step 1: Тесты и сборка на тестовой ноде**

```bash
git push -u origin feat/tma-panel
ssh dv@85.192.61.231 'git -C ~/ci/spirits_front fetch -q origin && git -C ~/ci/spirits_front checkout -q <sha>'
ssh dv@85.192.61.231 'source ~/.nvm/nvm.sh && cd ~/ci/spirits_front && pnpm install && pnpm test && pnpm build'
```

Expected: все сьюты зелёные, `tma-*.js` меньше 50 КБ

- [ ] **Step 2: Влить в main и выкатить**

```bash
git checkout main && git merge --no-ff feat/tma-panel && git push origin main
bash ~/Downloads/spirits_back/scripts/deploy.sh
```

Только с явного согласия владельца.

- [ ] **Step 3: Живая проверка**

Открыть мини-апп из меню бота на телефоне с русским профилем и английским языком системы: интерфейс обязан быть русским. Выбрать ассистента — окно не закрывается, появляется подтверждение; «Написать» возвращает в бота, где отвечает уже выбранный. В кошельке «Пополнить» открывает страницу оплаты в браузере.
