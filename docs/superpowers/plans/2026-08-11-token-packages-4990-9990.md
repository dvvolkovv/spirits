# Тарифы 4 990 ₽ и 9 990 ₽ — план реализации

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Добавить тарифы 4 990 ₽ / 3M токенов и 9 990 ₽ / 7M токенов с парными валютными пакетами «Приёма», сведя прайс к одному модулю на фронте.

**Architecture:** Новый модуль `src/config/tokenPackages.ts` — единственный источник правды для витрин. Юридические документы (оферта, таблица тарифов) остаются рукописными по действующему решению проекта, но страхуются сторожевым тестом, который сверяет их текст с модулем. Бэкенд получает два новых id в карте токенов и переработанный список валютных пакетов.

**Tech Stack:** React 18 + TypeScript + Vite, vitest (environment `node`), i18next (7 локалей), NestJS + jest на бэкенде.

**Спека:** `docs/superpowers/specs/2026-08-11-token-packages-4990-9990-design.md`

---

## Отклонение от спеки — прочитать до начала

Спека предписывала генерировать перечисление цен в оферте из массива. **Так делать не надо.**

`scripts/check-no-hardcoded.mjs` держит `LegalModal.tsx` и `PaymentInfoModal.tsx` в списке `DOCUMENT_BLOCKS` с обоснованием: юридические документы намеренно живут двумя рукописными блоками (RU и EN), потому что дробить обязывающий текст на ключи вредно, а машинно переводить его нельзя.

Поэтому: тексты правятся руками, а от устаревания их защищает сторожевой тест из Задачи 5. Остальное в спеке в силе.

## Структура файлов

| Файл | Ответственность |
|---|---|
| `src/config/tokenPackages.ts` | **создать** — прайс, ключи названий, базовая цена. Только данные, без React |
| `src/config/tokenPackages.test.ts` | **создать** — инварианты лестницы (монотонность, честность ярлыков) |
| `src/components/onboarding/documentPrices.test.ts` | **создать** — сверка рукописных документов с модулем |
| `src/components/tokens/TokenPackages.tsx` | модалка покупки: берёт прайс из модуля, раскладка на 5 колонок |
| `src/pages/TokenPurchasePage.tsx` | публичная страница покупки: то же самое |
| `src/components/onboarding/PaymentInfoModal.tsx` | таблица тарифов, два рукописных блока |
| `src/components/onboarding/LegalModal.tsx` | оферта, два рукописных блока |
| `src/i18n/locales/{ru,en,es,de,fr,pt,zh}.json` | три новых ключа |
| `spirits_back/src/payments/payments.service.ts` | карта `tokensForPackage` |
| `spirits_back/src/payments/priem.service.ts` | список `PRIEM_PACKAGES` |
| `spirits_back/src/payments/packages.spec.ts` | **создать** — инварианты обеих линеек на бэке |

Модуль намеренно не импортирует React и не зовёт `t()`: он отдаёт ключи, а переводит их потребитель. Это позволяет тестировать его в vitest с `environment: 'node'`, где React-компоненты не поднимаются.

---

## Задача 1: Модуль прайса

**Files:**
- Create: `src/config/tokenPackages.ts`
- Test: `src/config/tokenPackages.test.ts`

- [ ] **Шаг 1: Написать падающий тест**

Создать `src/config/tokenPackages.test.ts`:

```ts
import { describe, it, expect } from 'vitest';
import { RUB_PACKAGES, BASE_PRICE_PER_1000, CRYPTO_NAME_KEY } from './tokenPackages';

/**
 * Согласованный прайс, выписанный явно и отдельно от реализации.
 *
 * Смысл дублирования: правка цены в модуле «мимоходом» роняет этот тест, и
 * менять прайс приходится осознанно, в двух местах. Прайс — не тот код, где
 * незамеченная правка допустима.
 */
const EXPECTED: Array<[string, number, number]> = [
  ['starter', 149, 50_000],
  ['extended', 499, 200_000],
  ['professional', 1990, 1_000_000],
  ['business', 4990, 3_000_000],
  ['maximum', 9990, 7_000_000],
];

const pricePer1000 = (p: { priceRub: number; tokens: number }) => p.priceRub / (p.tokens / 1000);

describe('прайс токенов', () => {
  it('совпадает с согласованным списком', () => {
    expect(RUB_PACKAGES.map((p) => [p.id, p.priceRub, p.tokens])).toEqual(EXPECTED);
  });

  // Немонотонная лестница означает, что более крупная покупка невыгодна.
  // Именно так выглядел max_usd ($19.6 за миллион) рядом с business_usd ($20.7).
  it('цена за 1000 токенов строго убывает', () => {
    const prices = RUB_PACKAGES.map(pricePer1000);
    for (let i = 1; i < prices.length; i++) {
      expect(prices[i]).toBeLessThan(prices[i - 1]);
    }
  });

  it('ярлык скидки не превышает фактическую выгоду', () => {
    for (const p of RUB_PACKAGES) {
      if (p.savingsPct === undefined) continue;
      const actual = (1 - pricePer1000(p) / BASE_PRICE_PER_1000) * 100;
      expect(p.savingsPct).toBeLessThanOrEqual(actual);
    }
  });

  it('ярлык скидки кратен пяти', () => {
    for (const p of RUB_PACKAGES) {
      if (p.savingsPct === undefined) continue;
      expect(p.savingsPct % 5).toBe(0);
    }
  });

  it('у базового пакета ярлыка скидки нет', () => {
    expect(RUB_PACKAGES[0].savingsPct).toBeUndefined();
  });

  it('у каждого пакета есть ключ названия и ключ объёма', () => {
    for (const p of RUB_PACKAGES) {
      expect(p.nameKey).toMatch(/^payment\./);
      expect(p.amountKey).toMatch(/^payment\./);
    }
  });

  it('плашка «популярный» ровно одна', () => {
    expect(RUB_PACKAGES.filter((p) => p.popular)).toHaveLength(1);
  });

  it('каждый валютный пакет витрины подписан', () => {
    for (const id of ['pro_usd', 'business_usd', 'maximum_usd']) {
      expect(CRYPTO_NAME_KEY[id]).toBeTruthy();
    }
  });
});
```

- [ ] **Шаг 2: Запустить тест и убедиться, что он падает**

Run: `pnpm test src/config/tokenPackages.test.ts`
Expected: FAIL — `Failed to resolve import "./tokenPackages"`.

- [ ] **Шаг 3: Написать модуль**

Создать `src/config/tokenPackages.ts`:

```ts
/**
 * Прайс токенов — единственный источник правды для витрин.
 *
 * До этого модуля прайс был выписан в пяти местах: две витрины, таблица в
 * PaymentInfoModal, перечисление в оферте LegalModal и карта tokensForPackage
 * на бэкенде. Копии разошлись: оферта перечисляла три пакета.
 *
 * Проценты скидок здесь — ЯРЛЫКИ, а не расчёт. Они округлены вниз до пятёрки,
 * чтобы обещание в интерфейсе оставалось заведомо выполнимым: у «Расширенного»
 * фактические 16.3% подписаны как 15%, у «Профессионального» 33.2% — как 30%.
 * Тест рядом следит, чтобы ярлык не обогнал фактическую выгоду.
 */

export interface RubPackage {
  id: string;
  priceRub: number;
  tokens: number;
  /** Ключ i18n для названия пакета. */
  nameKey: string;
  /**
   * Ключ i18n для строки вида «3 000 000 токенов». Число живёт внутри строки,
   * а не собирается из числа и слова: склонение «токенов» в семи языках
   * устроено по-разному, и склеивать его в коде некорректно.
   */
  amountKey: string;
  /** Ярлык скидки в процентах. У базового пакета отсутствует. */
  savingsPct?: number;
  popular?: boolean;
}

/** Базовая цена за 1000 токенов — стартовый пакет. От неё считаются скидки. */
export const BASE_PRICE_PER_1000 = 149 / 50;

export const RUB_PACKAGES: RubPackage[] = [
  {
    id: 'starter',
    priceRub: 149,
    tokens: 50_000,
    nameKey: 'payment.package_starter_name',
    amountKey: 'payment.info.package_basic_amount',
  },
  {
    id: 'extended',
    priceRub: 499,
    tokens: 200_000,
    nameKey: 'payment.info.package_extended',
    amountKey: 'payment.info.package_extended_amount',
    savingsPct: 15,
    popular: true,
  },
  {
    id: 'professional',
    priceRub: 1990,
    tokens: 1_000_000,
    nameKey: 'payment.info.package_pro',
    amountKey: 'payment.info.package_pro_amount',
    savingsPct: 30,
  },
  {
    id: 'business',
    priceRub: 4990,
    tokens: 3_000_000,
    nameKey: 'payment.info.package_business',
    amountKey: 'payment.info.package_business_amount',
    savingsPct: 40,
  },
  {
    id: 'maximum',
    priceRub: 9990,
    tokens: 7_000_000,
    // Ключ существует с тех пор, когда «Максимальным» назывался валютный
    // max_usd. Переведён во всех семи локалях — заводить второй незачем.
    nameKey: 'payment.package_max_name',
    amountKey: 'payment.info.package_maximum_amount',
    savingsPct: 50,
  },
];

/**
 * id валютного пакета → ключ названия. Сами пакеты (цена, объём) приходят с
 * бэкенда через /webhook/payments/methods; фронт знает только, как их назвать.
 *
 * max_usd снят с витрины 11.08.2026: рядом с business_usd он ломал
 * монотонность — $19.6 за миллион против $20.7, то есть более крупный пакет
 * оказывался невыгодным. Ключ оставлен: пакет может приехать из кеша старого
 * ответа бэкенда, и подписать его всё равно надо.
 */
export const CRYPTO_NAME_KEY: Record<string, string> = {
  pro_usd: 'payment.info.package_pro',
  business_usd: 'payment.info.package_business',
  maximum_usd: 'payment.package_max_name',
  max_usd: 'payment.package_max_name',
};

/** Чем подписать валютный пакет с незнакомым id. */
export const CRYPTO_NAME_KEY_FALLBACK = 'payment.info.package_pro';
```

- [ ] **Шаг 4: Запустить тест и убедиться, что он проходит**

Run: `pnpm test src/config/tokenPackages.test.ts`
Expected: PASS, 8 тестов.

- [ ] **Шаг 5: Сломать нарочно и убедиться, что тест это ловит**

Временно поменять в модуле `savingsPct: 40` на `savingsPct: 45` у `business`.

Run: `pnpm test src/config/tokenPackages.test.ts`
Expected: FAIL — «ярлык скидки не превышает фактическую выгоду» (45 > 44.18).

Вернуть `40`, снова прогнать — PASS. Зелёный тест, который не умеет краснеть, ничего не проверяет.

- [ ] **Шаг 6: Коммит**

```bash
git add src/config/tokenPackages.ts src/config/tokenPackages.test.ts
git commit -m "feat(tokens): прайс сведён в один модуль

Пять копий прайса разошлись — оферта перечисляла три пакета. Модуль
становится источником правды для витрин; инварианты лестницы (монотонность
цены за токен, честность ярлыка скидки) закреплены тестами."
```

---

## Задача 2: Ключи локализации

**Files:**
- Modify: `src/i18n/locales/ru.json`, `en.json`, `es.json`, `de.json`, `fr.json`, `pt.json`, `zh.json`

Три новых ключа. `payment.package_max_name` уже существует во всех локалях («Максимальный», Maximum, Máximo, 至尊版) — переиспользуется.

- [ ] **Шаг 1: Убедиться, что ключей ещё нет**

Run: `grep -c "package_business" src/i18n/locales/*.json`
Expected: `0` в каждом файле.

- [ ] **Шаг 2: Добавить ключи в блок `payment.info` каждой локали**

Значения объёмов копируют разделитель разрядов и слово, уже принятые в этой локали для `package_pro_amount`.

`ru.json`:
```json
"package_business": "Бизнес",
"package_business_amount": "3 000 000 токенов",
"package_maximum_amount": "7 000 000 токенов",
```

`en.json`:
```json
"package_business": "Business",
"package_business_amount": "3,000,000 tokens",
"package_maximum_amount": "7,000,000 tokens",
```

`de.json`:
```json
"package_business": "Business",
"package_business_amount": "3.000.000 Tokens",
"package_maximum_amount": "7.000.000 Tokens",
```

`es.json`:
```json
"package_business": "Negocio",
"package_business_amount": "3.000.000 tokens",
"package_maximum_amount": "7.000.000 tokens",
```

`fr.json`:
```json
"package_business": "Entreprise",
"package_business_amount": "3 000 000 jetons",
"package_maximum_amount": "7 000 000 jetons",
```

`pt.json`:
```json
"package_business": "Empresarial",
"package_business_amount": "3 000 000 tokens",
"package_maximum_amount": "7 000 000 tokens",
```

`zh.json`:
```json
"package_business": "商务版",
"package_business_amount": "3,000,000 代币",
"package_maximum_amount": "7,000,000 代币",
```

- [ ] **Шаг 3: Проверить полноту и формат локалей**

Run: `pnpm check-locales && pnpm check-locale-format`
Expected: обе проверки зелёные. `check-locales` падает, если ключ есть в одной локали и отсутствует в другой.

- [ ] **Шаг 4: Проверить, что все ключи модуля существуют**

Run: `pnpm check-keys`
Expected: PASS.

- [ ] **Шаг 5: Коммит**

```bash
git add src/i18n/locales
git commit -m "i18n(tokens): названия и объёмы пакетов «Бизнес» и «Максимальный»

Ключ package_max_name переиспользован — он переведён во всех семи локалях
ещё со времён валютного max_usd."
```

---

## Задача 3: Витрина в модалке покупки

**Files:**
- Modify: `src/components/tokens/TokenPackages.tsx`

- [ ] **Шаг 1: Заменить локальный список на импорт**

Добавить в импорты сверху файла:

```ts
import { RUB_PACKAGES, CRYPTO_NAME_KEY, CRYPTO_NAME_KEY_FALLBACK } from '../../config/tokenPackages';
```

Найти локальное объявление `const getPackages = (t: ...) => [...]` (массив из трёх пакетов) и заменить целиком на:

```ts
const getPackages = (t: (key: string, opts?: Record<string, unknown>) => string): TokenPackage[] =>
  RUB_PACKAGES.map((p) => ({
    id: p.id,
    name: t(p.nameKey),
    tokens: p.tokens,
    price: p.priceRub,
    popular: p.popular,
    savings: p.savingsPct === undefined
      ? undefined
      : t('payment.package_savings', { percent: p.savingsPct }),
  }));
```

- [ ] **Шаг 2: Удалить локальную копию карты названий**

Найти внутри компонента объявление:

```ts
  const CRYPTO_NAME_KEY: Record<string, string> = {
    pro_usd: 'payment.info.package_pro',
    max_usd: 'payment.package_max_name',
  };
```

Удалить его целиком — карта теперь приходит импортом. В месте использования заменить fallback:

```ts
        name: t(CRYPTO_NAME_KEY[p.id] ?? CRYPTO_NAME_KEY_FALLBACK),
```

- [ ] **Шаг 3: Раскладка на пять колонок**

Найти `<div className="grid md:grid-cols-3 gap-6">` и заменить на:

```tsx
          <div className="grid md:grid-cols-3 lg:grid-cols-5 gap-6">
```

- [ ] **Шаг 4: Проверить типы и сборку**

Run: `pnpm typecheck && pnpm build`
Expected: обе команды успешны.

- [ ] **Шаг 5: Коммит**

```bash
git add src/components/tokens/TokenPackages.tsx
git commit -m "feat(tokens): витрина покупки берёт прайс из модуля

Пять тарифов на широком экране укладываются в один ряд — лестница скидок
читается целиком."
```

---

## Задача 4: Витрина на публичной странице

**Files:**
- Modify: `src/pages/TokenPurchasePage.tsx`

Файл содержит полную копию и `getPackages`, и `CRYPTO_NAME_KEY`. Правки те же, что в Задаче 3, но пути импорта другие — код повторён здесь целиком, чтобы задачу можно было выполнять отдельно.

- [ ] **Шаг 1: Заменить локальный список на импорт**

Добавить в импорты:

```ts
import { RUB_PACKAGES, CRYPTO_NAME_KEY, CRYPTO_NAME_KEY_FALLBACK } from '../config/tokenPackages';
```

Заменить объявление `const getPackages = ...` (строки 29–51, массив из трёх пакетов) на:

```ts
const getPackages = (t: (key: string, opts?: Record<string, unknown>) => string): TokenPackage[] =>
  RUB_PACKAGES.map((p) => ({
    id: p.id,
    name: t(p.nameKey),
    tokens: p.tokens,
    price: p.priceRub,
    popular: p.popular,
    savings: p.savingsPct === undefined
      ? undefined
      : t('payment.package_savings', { percent: p.savingsPct }),
  }));
```

- [ ] **Шаг 2: Удалить локальную копию карты названий**

Удалить внутри компонента:

```ts
  const CRYPTO_NAME_KEY: Record<string, string> = {
    pro_usd: 'payment.info.package_pro',
    max_usd: 'payment.package_max_name',
  };
```

и заменить использование на:

```ts
        name: t(CRYPTO_NAME_KEY[p.id] ?? CRYPTO_NAME_KEY_FALLBACK),
```

- [ ] **Шаг 3: Раскладка на пять колонок**

Найти `<div className="grid md:grid-cols-3 gap-6 mb-8">` и заменить на:

```tsx
            <div className="grid md:grid-cols-3 lg:grid-cols-5 gap-6 mb-8">
```

- [ ] **Шаг 4: Проверить типы и сборку**

Run: `pnpm typecheck && pnpm build`
Expected: успех.

- [ ] **Шаг 5: Коммит**

```bash
git add src/pages/TokenPurchasePage.tsx
git commit -m "feat(tokens): публичная страница покупки берёт прайс из модуля

Копия getPackages и карты названий удалена — обе витрины теперь питаются
одним массивом."
```

---

## Задача 5: Документы и сторожевой тест

Оферта и таблица тарифов правятся руками: оба файла числятся в `DOCUMENT_BLOCKS` скрипта `check-no-hardcoded`, потому что обязывающий текст нельзя ни дробить на ключи, ни переводить машинно. От устаревания их страхует тест.

**Files:**
- Create: `src/components/onboarding/documentPrices.test.ts`
- Modify: `src/components/onboarding/PaymentInfoModal.tsx`
- Modify: `src/components/onboarding/LegalModal.tsx`

- [ ] **Шаг 1: Написать падающий тест**

Создать `src/components/onboarding/documentPrices.test.ts`:

```ts
import { describe, it, expect } from 'vitest';
import { readFileSync } from 'node:fs';
import { RUB_PACKAGES } from '../../config/tokenPackages';

/**
 * Оферта и таблица тарифов — рукописные документы: они перечислены в
 * DOCUMENT_BLOCKS скрипта check-no-hardcoded, потому что дробить обязывающий
 * текст на ключи вредно, а переводить его машинно нельзя.
 *
 * Рукописный — значит отстающий: до этой проверки оферта перечисляла три
 * пакета, когда в витрине их стало пять. Тест не пишет текст за человека, но
 * не даёт забыть пакет: каждый объём и каждая цена обязаны встречаться в обоих
 * документах, в русском и английском написании.
 */

const read = (p: string) => readFileSync(p, 'utf8').replace(/ /g, ' ');

const DOCUMENTS = {
  оферта: read('src/components/onboarding/LegalModal.tsx'),
  'таблица тарифов': read('src/components/onboarding/PaymentInfoModal.tsx'),
};

/** «3 000 000» — как пишут по-русски и по-французски. */
const ru = (n: number) => n.toLocaleString('ru-RU').replace(/ /g, ' ');
/** «3,000,000» — как пишут по-английски. */
const en = (n: number) => n.toLocaleString('en-US');

describe('цены в рукописных документах', () => {
  for (const [docName, text] of Object.entries(DOCUMENTS)) {
    for (const p of RUB_PACKAGES) {
      it(`${docName}: пакет ${p.id} указан объёмом`, () => {
        expect(text).toContain(ru(p.tokens));
        expect(text).toContain(en(p.tokens));
      });

      it(`${docName}: пакет ${p.id} указан ценой`, () => {
        expect(text).toContain(ru(p.priceRub));
        expect(text).toContain(en(p.priceRub));
      });
    }
  }
});
```

- [ ] **Шаг 2: Запустить тест и убедиться, что он падает**

Run: `pnpm test src/components/onboarding/documentPrices.test.ts`
Expected: FAIL — 8 тестов из 20 красные: `business` и `maximum` не найдены ни в оферте, ни в таблице. Три существующих пакета проходят, что подтверждает: тест ищет там, где надо.

- [ ] **Шаг 3: Дополнить таблицу тарифов, русский блок**

В `PaymentInfoModal.tsx` после строки с `package_pro` (`1 000 000 токенов` / `1 990 ₽`) добавить две строки:

```tsx
              <tr className="hover:bg-gray-50"><td className="px-4 py-3 font-medium text-gray-900">{t('payment.info.package_business', 'Бизнес')}</td><td className="px-4 py-3 text-gray-700">{t('payment.info.package_business_amount', '3 000 000 токенов')}</td><td className="px-4 py-3 text-forest-600 font-semibold">4 990 ₽</td></tr>
              <tr className="hover:bg-gray-50"><td className="px-4 py-3 font-medium text-gray-900">{t('payment.package_max_name', 'Максимальный')}</td><td className="px-4 py-3 text-gray-700">{t('payment.info.package_maximum_amount', '7 000 000 токенов')}</td><td className="px-4 py-3 text-forest-600 font-semibold">9 990 ₽</td></tr>
```

- [ ] **Шаг 4: Дополнить таблицу тарифов, английский блок**

В том же файле, после строки `Professional` / `1,000,000 tokens` / `1,990 ₽` добавить:

```tsx
              <tr className="hover:bg-gray-50"><td className="px-4 py-3 font-medium text-gray-900">Business</td><td className="px-4 py-3 text-gray-700">3,000,000 tokens</td><td className="px-4 py-3 text-forest-600 font-semibold">4,990 ₽</td></tr>
              <tr className="hover:bg-gray-50"><td className="px-4 py-3 font-medium text-gray-900">Maximum</td><td className="px-4 py-3 text-gray-700">7,000,000 tokens</td><td className="px-4 py-3 text-forest-600 font-semibold">9,990 ₽</td></tr>
```

- [ ] **Шаг 5: Дополнить оферту, русский текст**

В `LegalModal.tsx` заменить абзац о пакетах:

```tsx
        <p className="text-gray-700 leading-relaxed">
          При регистрации пользователю единовременно начисляется приветственный бонус —
          25 000 токенов. Дополнительные токены приобретаются пакетами: 50 000 токенов —
          149 ₽, 200 000 токенов — 499 ₽, 1 000 000 токенов — 1 990 ₽, 3 000 000 токенов —
          4 990 ₽, 7 000 000 токенов — 9 990 ₽. Актуальный состав пакетов и цены
          отображаются в Приложении в разделе покупки токенов.
        </p>
```

- [ ] **Шаг 6: Дополнить оферту, английский текст**

В том же файле заменить соответствующий абзац:

```tsx
        <p className="text-gray-700 leading-relaxed">
          Upon registration the user receives a one-time welcome bonus of 25,000 tokens.
          Additional tokens are purchased in packages: 50,000 tokens — RUB 149; 200,000
          tokens — RUB 499; 1,000,000 tokens — RUB 1,990; 3,000,000 tokens — RUB 4,990;
          7,000,000 tokens — RUB 9,990. The current package composition and prices are
          displayed in the Application in the token purchase section.
        </p>
```

- [ ] **Шаг 7: Запустить тест и убедиться, что он проходит**

Run: `pnpm test src/components/onboarding/documentPrices.test.ts`
Expected: PASS, 20 тестов.

- [ ] **Шаг 8: Проверить, что сторож хардкода не сломался**

Run: `pnpm check-hardcoded`
Expected: PASS. Оба файла в `DOCUMENT_BLOCKS`, поэтому русский текст в них разрешён.

- [ ] **Шаг 9: Коммит**

```bash
git add src/components/onboarding/documentPrices.test.ts src/components/onboarding/PaymentInfoModal.tsx src/components/onboarding/LegalModal.tsx
git commit -m "feat(tokens): новые тарифы в оферте и таблице + сторож от расхождения

Юридический текст остаётся рукописным по решению из check-no-hardcoded, но
тест теперь требует, чтобы каждый пакет из прайса встречался в обоих
документах в русском и английском написании. Именно оферта отставала."
```

---

## Задача 6: Бэкенд

**Files:**
- Modify: `~/Downloads/spirits_back/src/payments/payments.service.ts`
- Modify: `~/Downloads/spirits_back/src/payments/priem.service.ts`
- Create: `~/Downloads/spirits_back/src/payments/packages.spec.ts`

Все команды этой задачи выполняются из `~/Downloads/spirits_back`.

- [ ] **Шаг 1: Написать падающий тест**

Создать `src/payments/packages.spec.ts`:

```ts
import { PRIEM_PACKAGES, PriemService } from './priem.service';
import { PaymentsService } from './payments.service';

/**
 * Прайс живёт на фронте, а число начисляемых токенов — здесь. Репозитории
 * разные, и карта легко отстанет от витрины. Ожидаемая таблица выписана явно:
 * правка карты без правки таблицы роняет сборку.
 */
const EXPECTED_RUB: Array<[string, number]> = [
  ['starter', 50_000],
  ['extended', 200_000],
  ['professional', 1_000_000],
  ['business', 3_000_000],
  ['maximum', 7_000_000],
];

describe('пакеты токенов', () => {
  describe('рублёвая карта', () => {
    // tokensForPackage приватный: он часть контракта оплаты, а не публичный API.
    const tokensFor = (id: string): number =>
      (PaymentsService.prototype as any).tokensForPackage.call(null, id, 0);

    for (const [id, tokens] of EXPECTED_RUB) {
      it(`${id} даёт ${tokens} токенов`, () => {
        expect(tokensFor(id)).toBe(tokens);
      });
    }

    it('незнакомый пакет не даёт ноль — падает на формулу от суммы', () => {
      const byAmount = (PaymentsService.prototype as any).tokensForPackage.call(null, 'нет-такого', 7);
      expect(byAmount).toBe(7000);
    });
  });

  describe('валютная линейка', () => {
    it('состоит из трёх пакетов', () => {
      expect(PRIEM_PACKAGES.map((p) => p.id)).toEqual(['pro_usd', 'business_usd', 'maximum_usd']);
    });

    // Тот же инвариант, что на фронте: более крупный пакет обязан быть выгоднее.
    it('цена за миллион токенов строго убывает', () => {
      const perMillion = PRIEM_PACKAGES.map((p) => p.usd / (p.tokens / 1_000_000));
      for (let i = 1; i < perMillion.length; i++) {
        expect(perMillion[i]).toBeLessThan(perMillion[i - 1]);
      }
    });

    // Снят с витрины: $19.6 за миллион против $20.7 у business_usd ломало
    // монотонность и делало более крупную покупку невыгодной.
    it('max_usd больше не заказывается', () => {
      expect(PRIEM_PACKAGES.find((p) => p.id === 'max_usd')).toBeUndefined();
    });

    it('оба новых пакета проходят порог оплаты картой', () => {
      for (const p of PRIEM_PACKAGES) {
        expect(p.usd).toBeGreaterThanOrEqual(10);
      }
    });
  });
});
```

- [ ] **Шаг 2: Запустить тест и убедиться, что он падает**

Run: `npx jest src/payments/packages.spec.ts`
Expected: FAIL — `business`/`maximum` возвращают не те числа, а `PRIEM_PACKAGES` содержит `max_usd`.

- [ ] **Шаг 3: Дополнить карту токенов**

В `src/payments/payments.service.ts` в методе `tokensForPackage` заменить объект `map` на:

```ts
    const map: Record<string, number> = {
      basic: 50000,
      starter: 50000,
      standard: 200000,
      extended: 200000,
      premium: 1000000,
      professional: 1000000,
      business: 3_000_000,
      maximum: 7_000_000,
    };
```

- [ ] **Шаг 4: Переработать валютную линейку**

В `src/payments/priem.service.ts` заменить `PRIEM_PACKAGES` на:

```ts
export const PRIEM_PACKAGES: PriemPackage[] = [
  { id: 'pro_usd', tokens: 1_000_000, usd: 25 },
  { id: 'business_usd', tokens: 3_000_000, usd: 62 },
  { id: 'maximum_usd', tokens: 7_000_000, usd: 125 },
];
```

Там же дополнить комментарий над списком, чтобы следующий читатель не вернул `max_usd` обратно:

```ts
 * max_usd за $49/2.5M снят 11.08.2026: рядом с business_usd он давал $19.6 за
 * миллион против $20.7, то есть более крупная покупка становилась невыгодной.
 * Висевшие на момент снятия счета не пострадали — зачисление читает число
 * токенов из строки payments, а не из этого списка.
```

- [ ] **Шаг 5: Запустить тест и убедиться, что он проходит**

Run: `npx jest src/payments/packages.spec.ts`
Expected: PASS, 10 тестов.

- [ ] **Шаг 6: Сломать нарочно**

Временно вернуть в `PRIEM_PACKAGES` строку `{ id: 'max_usd', tokens: 2_500_000, usd: 49 }` вторым элементом.

Run: `npx jest src/payments/packages.spec.ts`
Expected: FAIL — «цена за миллион токенов строго убывает» и «max_usd больше не заказывается».

Убрать строку, прогнать снова — PASS.

- [ ] **Шаг 7: Прогнать все тесты платежей**

Run: `npx jest src/payments/`
Expected: PASS, включая существующий `priem.service.spec.ts`. Он заказывает только `pro_usd` (строки 270 и 286) и снятия `max_usd` не замечает — править его не нужно.

- [ ] **Шаг 8: Коммит**

```bash
git add src/payments/payments.service.ts src/payments/priem.service.ts src/payments/packages.spec.ts
git commit -m "feat(payments): пакеты business и maximum, валютная линейка перестроена

Рублёвая карта дополнена 3M и 7M. В валютной линейке max_usd за \$49/2.5M
заменён на business_usd (\$62/3M) и maximum_usd (\$125/7M): рядом с новым
пакетом старый ломал монотонность и делал крупную покупку невыгодной.

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>"
```

---

## Задача 7: Сквозная проверка

**Files:** ничего не меняется — только проверки.

- [ ] **Шаг 1: Полный прогон фронта**

Run: `pnpm test && pnpm typecheck && pnpm build && pnpm check-locales && pnpm check-keys && pnpm check-hardcoded && pnpm check-locale-format`
Expected: все семь команд успешны.

- [ ] **Шаг 2: Полный прогон бэка**

Run (из `~/Downloads/spirits_back`): `npx jest src/payments/ src/admin/ && npm run build`
Expected: PASS, сборка успешна.

- [ ] **Шаг 3: Глазами — витрина**

Run: `pnpm dev`, открыть `/tokens`.

Проверить на трёх ширинах окна:
- ≥1024px — пять карточек одним рядом, ярлыки 15% / 30% / 40% / 50%;
- 768–1023px — 3 + 2;
- <768px — столбиком.

Плашка «популярный» — на пакете за 499 ₽ и больше нигде.

- [ ] **Шаг 4: Глазами — документы**

Открыть модалку условий оплаты и оферту, переключая язык интерфейса между русским и английским. В таблице пять строк, в перечислении оферты — пять пакетов, в обоих языках.

- [ ] **Шаг 5: Глазами — валютная витрина**

Переключить язык профиля на английский, открыть покупку токенов. Витрина должна показать три долларовых пакета — $25 / $62 / $125 — с названиями Professional, Business, Maximum. Пакета за $49 быть не должно.

- [ ] **Шаг 6: Готовность к деплою**

Своих коммитов у этой задачи нет — всё закоммичено по ходу Задач 1–6. Убедиться, что рабочее дерево чистое:

Run: `git status --short`
Expected: пусто.

Деплой — **только** через `bash ~/Downloads/spirits_back/scripts/deploy.sh` и **только** по явному согласованию с владельцем: скрипт катит на прод. Ручной `rsync` или сборка на сервере запрещены политикой в `CLAUDE.md`.

---

## Что этот план осознанно не делает

- Лендинг `land_linkeon` — другой репозиторий со своим выкатом; цены там наверняка тоже продублированы, и это отдельная работа.
- Таблица `token_packages` в базе остаётся пустой; смена цен без деплоя — отдельный проект.
- Восемь мест, где `SUM(amount)` складывает рубли с долларами (VMM, мониторинг экономики, атрибуция, персоны, карточка пользователя) — соседняя тема, к тарифам отношения не имеет.
