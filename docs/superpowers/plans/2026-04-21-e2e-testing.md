# E2E-тестирование my.linkeon.io — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Добавить Playwright-слой E2E-тестов (10 flows) поверх существующих 32 API + 18 backend E2E тестов, покрывая все критические пользовательские пути my.linkeon.io на live prod-среде.

**Architecture:** Playwright-проект в `spirits_front/tests/e2e/`, запуск через `pnpm test:e2e`. Авторизация один раз через `globalSetup` → storageState для двух ролей. Необратимые операции (YooKassa, баланс токенов) моккаются либо идут через новый debug-эндпоинт бэкенда. Каждый flow идемпотентен (cleanup через API/SSH после теста).

**Tech Stack:**
- Playwright 1.50+ (TypeScript)
- pnpm
- Существующий NestJS бэкенд на `82.202.197.230:60322`
- Debug endpoints под env-гейтом `DEBUG_SMS_CODES=true`

**Spec:** [docs/superpowers/specs/2026-04-21-e2e-testing-design.md](../specs/2026-04-21-e2e-testing-design.md)

---

## Структура файлов (map)

### Фронтенд (`spirits_front/`)

**Создать:**
- `playwright.config.ts` — конфиг Playwright (root)
- `tests/e2e/helpers/testData.ts` — константы: телефоны, ассистенты, URL
- `tests/e2e/helpers/guards.ts` — `assertIsTestPhone` + `TEST_PHONES` + pattern для референс-номеров
- `tests/e2e/helpers/guards.test.ts` — unit-тесты guard-ов
- `tests/e2e/helpers/otp.ts` — `fetchOtp(phone)` → GET /webhook/debug/sms-code
- `tests/e2e/helpers/login.ts` — `loginViaApi(phone)` → JWT + userData
- `tests/e2e/helpers/mockYookassa.ts` — `mockYookassaCheckout(page)`
- `tests/e2e/helpers/cleanup.ts` — `resetProfile`, `clearChatHistory`, `resetTokens`, `deleteCoupon`
- `tests/e2e/fixtures/auth.fixture.ts` — Playwright test.extend с auth role
- `tests/e2e/global-setup.ts` — один раз логинит два аккаунта, пишет `.auth/*.json`
- `tests/e2e/flows/01-onboarding.spec.ts`
- `tests/e2e/flows/02-chat.spec.ts`
- `tests/e2e/flows/03-assistant-switch.spec.ts`
- `tests/e2e/flows/04-profile.spec.ts`
- `tests/e2e/flows/05-tokens-purchase.spec.ts`
- `tests/e2e/flows/06-coupon.spec.ts`
- `tests/e2e/flows/07-search-compatibility.spec.ts`
- `tests/e2e/flows/08-admin.spec.ts`
- `tests/e2e/flows/09-referral.spec.ts`
- `tests/e2e/flows/10-mobile-layout.spec.ts`
- `tests/e2e/README.md`
- `tests/e2e/.gitignore` — игнорит `.auth/`, `test-results/`, `playwright-report/`

**Модифицировать (добавить `data-testid`):**
- `src/components/onboarding/PhoneInput.tsx`
- `src/components/onboarding/OTPInput.tsx`
- `src/components/onboarding/LegalModal.tsx`
- `src/components/chat/ChatInterface.tsx`
- `src/components/chat/AssistantSelection.tsx`
- `src/components/profile/ProfileView.tsx`
- `src/components/tokens/TokenPackages.tsx`
- `src/components/tokens/CouponInput.tsx`
- `src/components/search/SearchInterface.tsx`
- `src/components/search/CompatibilityInterface.tsx`
- `src/components/search/UserProfileModal.tsx`
- `src/components/admin/AdminAssistantsView.tsx`
- `src/components/admin/AdminCouponsView.tsx`
- `src/components/admin/AdminReferralsView.tsx`
- `src/components/layout/Navigation.tsx`
- `package.json` — scripts: `test:e2e`, `test:e2e:ui`, `test:e2e:debug`, `test:e2e:headed` + devDep `@playwright/test`
- `CLAUDE.md` — секция Playwright tests
- `.gitignore` — `/test-results/`, `/playwright-report/`, `/tests/e2e/.auth/`

### Бэкенд (`spirits_back/`)

**Модифицировать:**
- `src/auth/auth.controller.ts` — добавить `POST /webhook/debug/add-tokens/:phone/:amount` + ужесточить guard для существующего `debug/sms-code/:phone` (whitelist + pattern)
- `src/auth/auth.service.ts` — метод `debugAddTokens(phone, amount)`

---

## Task 1: Установка Playwright и скелет конфига

**Files:**
- Modify: `spirits_front/package.json`
- Create: `spirits_front/playwright.config.ts`
- Create: `spirits_front/tests/e2e/.gitignore`
- Modify: `spirits_front/.gitignore`

- [ ] **Step 1.1: Установить Playwright**

```bash
cd ~/Downloads/spirits_front
pnpm add -D @playwright/test@^1.50.0
pnpm exec playwright install chromium webkit
```

Expected: установка завершается, chromium и webkit загружены.

- [ ] **Step 1.2: Добавить scripts в package.json**

Отредактировать `package.json`, секция `"scripts"`:

```json
{
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "lint": "eslint .",
    "preview": "vite preview",
    "test:e2e": "playwright test",
    "test:e2e:ui": "playwright test --ui",
    "test:e2e:debug": "PWDEBUG=1 playwright test",
    "test:e2e:headed": "playwright test --headed"
  }
}
```

- [ ] **Step 1.3: Создать skeleton `playwright.config.ts`**

```ts
import { defineConfig, devices } from '@playwright/test';

export default defineConfig({
  testDir: './tests/e2e',
  testMatch: /.*\.(test|spec)\.ts/,
  timeout: 60_000,
  expect: { timeout: 15_000 },
  fullyParallel: false,
  workers: 1,
  retries: process.env.CI ? 2 : 2,
  reporter: [['list'], ['html', { open: 'never' }]],
  use: {
    baseURL: 'https://my.linkeon.io',
    actionTimeout: 15_000,
    trace: 'on-first-retry',
    video: 'retain-on-failure',
    screenshot: 'only-on-failure',
  },
  globalSetup: './tests/e2e/global-setup.ts',
  projects: [
    {
      name: 'chromium-user',
      use: {
        ...devices['Desktop Chrome'],
        storageState: './tests/e2e/.auth/test-user.json',
      },
    },
    {
      name: 'chromium-admin',
      use: {
        ...devices['Desktop Chrome'],
        storageState: './tests/e2e/.auth/test-admin.json',
      },
    },
    {
      name: 'chromium-anon',
      use: { ...devices['Desktop Chrome'] },
    },
    {
      name: 'mobile-user',
      use: {
        ...devices['iPhone 13'],
        storageState: './tests/e2e/.auth/test-user.json',
      },
    },
  ],
});
```

- [ ] **Step 1.4: Создать `tests/e2e/.gitignore`**

```
.auth/
test-results/
playwright-report/
playwright/.cache/
```

- [ ] **Step 1.5: Обновить корневой `.gitignore`**

Добавить в конец `spirits_front/.gitignore`:

```
# Playwright
/test-results/
/playwright-report/
/tests/e2e/.auth/
```

- [ ] **Step 1.6: Проверить, что Playwright обнаруживает конфиг**

```bash
cd ~/Downloads/spirits_front
pnpm exec playwright test --list
```

Expected: вывод "Listing tests" без ошибок (пока 0 тестов, это нормально; возможна ошибка отсутствия `global-setup.ts` — создадим в Task 4).

Если ругается на отсутствие `global-setup.ts` — временно закомментировать строку `globalSetup:` в конфиге, раскомментируем в Task 4.

- [ ] **Step 1.7: Commit**

```bash
cd ~/Downloads/spirits_front
git add package.json pnpm-lock.yaml playwright.config.ts tests/e2e/.gitignore .gitignore
git commit -m "chore: установка Playwright и скелет конфига для E2E"
```

---

## Task 2: Backend — debug-эндпоинт add-tokens + guard для sms-code

**Files:**
- Modify: `spirits_back/src/auth/auth.controller.ts`
- Modify: `spirits_back/src/auth/auth.service.ts`

### Подзадачи

- [ ] **Step 2.1: Добавить метод `debugAddTokens` в `auth.service.ts`**

В файле `~/Downloads/spirits_back/src/auth/auth.service.ts` найти конец класса `AuthService` и добавить метод:

```ts
// Debug: изменить баланс токенов тестового пользователя (+/-).
// Используется ТОЛЬКО Playwright-тестами; гейт по DEBUG_SMS_CODES в контроллере.
async debugAddTokens(phone: string, delta: number): Promise<{
  phone: string;
  balance_before: number;
  balance_after: number;
}> {
  const client = await this.pool.connect();
  try {
    await client.query('BEGIN');
    const before = await client.query(
      'SELECT tokens FROM users WHERE phone = $1 FOR UPDATE',
      [phone],
    );
    if (before.rows.length === 0) {
      throw new Error(`user not found: ${phone}`);
    }
    const balanceBefore = Number(before.rows[0].tokens || 0);
    const balanceAfter = balanceBefore + delta;
    await client.query(
      'UPDATE users SET tokens = $1 WHERE phone = $2',
      [balanceAfter, phone],
    );
    await client.query('COMMIT');
    return { phone, balance_before: balanceBefore, balance_after: balanceAfter };
  } catch (err) {
    await client.query('ROLLBACK');
    throw err;
  } finally {
    client.release();
  }
}
```

> **Примечание:** если в `AuthService` поле называется не `this.pool` а, например, `this.db` или используется через отдельный DbService — адаптировать под существующий pattern в этом же файле. Первым делом прочитать существующий метод (например, `getDebugCode`) и использовать тот же способ обращения к БД.

- [ ] **Step 2.2: Добавить контроллер-метод `debugAddTokens` в `auth.controller.ts`**

В файле `~/Downloads/spirits_back/src/auth/auth.controller.ts`, после существующего `debugSmsCode` (около строки 65):

```ts
// Debug: изменить баланс тестового пользователя (только для E2E-тестов).
@Post('debug/add-tokens/:phone/:amount')
async debugAddTokens(
  @Param('phone') phone: string,
  @Param('amount') amount: string,
  @Res() res: Response,
) {
  if (process.env.DEBUG_SMS_CODES !== 'true') {
    return res.status(404).json({ error: 'Not found' });
  }
  if (!this.isTestPhone(phone)) {
    return res.status(403).json({ error: 'Phone not in whitelist' });
  }
  const delta = parseInt(amount, 10);
  if (Number.isNaN(delta)) {
    return res.status(400).json({ error: 'Invalid amount' });
  }
  try {
    const result = await this.authService.debugAddTokens(phone, delta);
    return res.status(200).json(result);
  } catch (err) {
    return res.status(500).json({ error: err.message || 'Internal error' });
  }
}

// Whitelist тестовых телефонов для всех debug-эндпоинтов.
// Фиксированный список + pattern для динамических referral-аккаунтов.
private isTestPhone(phone: string): boolean {
  const FIXED = ['70000000000', '79030169187'];
  if (FIXED.includes(phone)) return true;
  return /^790300\d{5}$/.test(phone);
}
```

- [ ] **Step 2.3: Ужесточить существующий `debugSmsCode` тем же guard-ом**

В `auth.controller.ts` метод `debugSmsCode`, после проверки `DEBUG_SMS_CODES` добавить проверку whitelist:

```ts
@Get('debug/sms-code/:phone')
async debugSmsCode(@Param('phone') phone: string, @Res() res: Response) {
  if (process.env.DEBUG_SMS_CODES !== 'true') {
    return res.status(404).json({ error: 'Not found' });
  }
  if (!this.isTestPhone(phone)) {
    return res.status(403).json({ error: 'Phone not in whitelist' });
  }
  const code = await this.authService.getDebugCode(phone);
  if (!code) return res.status(404).json({ error: 'No code' });
  return res.status(200).json({ code });
}
```

- [ ] **Step 2.4: Импортнуть `Post` и `Param` в контроллер**

Проверить верхний импорт `@nestjs/common` — должны быть оба: `Post`, `Param`. Обычно они уже есть в строке `import { Controller, Post, Get, Delete, Req, Res, UseGuards, Query, Param } from '@nestjs/common';`. Если `Param` отсутствует — добавить.

- [ ] **Step 2.5: Локальная сборка для проверки TS-ошибок**

```bash
cd ~/Downloads/spirits_back
npm run build
```

Expected: `dist/` собирается без ошибок TypeScript.

- [ ] **Step 2.6: Деплой на сервер + рестарт**

```bash
cd ~/Downloads/spirits_back
rsync -az -e "ssh -p 60322" src/ dvolkov@82.202.197.230:~/spirits_back/src/
ssh -p 60322 dvolkov@82.202.197.230 "cd ~/spirits_back && npm run build && pm2 restart linkeon-api"
```

Expected: `pm2 restart` возвращает `[PM2] Applying action restartProcessId...` и `online`.

- [ ] **Step 2.7: Smoke-проверка обоих debug-эндпоинтов через curl**

```bash
# sms-code: должно вернуть 403 для не-тестового номера
curl -s -o /dev/stdout -w "\nHTTP %{http_code}\n" "https://my.linkeon.io/webhook/debug/sms-code/79999999999"

# sms-code: должно вернуть JSON с code (или 404 если код не запрашивали)
curl -s "https://my.linkeon.io/webhook/debug/sms-code/70000000000"

# add-tokens: +0 (no-op) под тестовым номером — должно вернуть balance_before == balance_after
curl -s -X POST "https://my.linkeon.io/webhook/debug/add-tokens/70000000000/0"

# add-tokens: 403 для чужого номера
curl -s -o /dev/stdout -w "\nHTTP %{http_code}\n" -X POST "https://my.linkeon.io/webhook/debug/add-tokens/79999999999/1000"
```

Expected:
- Первый запрос: `{"error":"Phone not in whitelist"}` HTTP 403
- Второй: `{"code":"1234"}` (если код есть в Redis) либо `{"error":"No code"}`
- Третий: `{"phone":"70000000000","balance_before":N,"balance_after":N}`
- Четвёртый: HTTP 403

- [ ] **Step 2.8: Commit (в `spirits_back` репо)**

```bash
cd ~/Downloads/spirits_back
git add src/auth/auth.controller.ts src/auth/auth.service.ts
git commit -m "feat(auth): debug-эндпоинт /debug/add-tokens + whitelist телефонов для debug-ручек

Нужен для Playwright E2E-тестов на prod: безопасное изменение баланса тестового
пользователя после мок-YooKassa. Гейтится DEBUG_SMS_CODES=true + whitelist
телефонов (фикс. список + паттерн 790300XXXXX для referral-тестов)."
git push
```

---

## Task 3: Helpers foundation (testData, guards, guards-тесты)

**Files:**
- Create: `spirits_front/tests/e2e/helpers/testData.ts`
- Create: `spirits_front/tests/e2e/helpers/guards.ts`
- Create: `spirits_front/tests/e2e/helpers/guards.test.ts`

- [ ] **Step 3.1: `tests/e2e/helpers/testData.ts`**

```ts
export const BASE_URL = 'https://my.linkeon.io';

export const TEST_PHONES = {
  USER: '70000000000',
  ADMIN: '79030169187',
} as const;

export const AUTH_STATE_PATH = {
  USER: './tests/e2e/.auth/test-user.json',
  ADMIN: './tests/e2e/.auth/test-admin.json',
} as const;

export const TIMEOUTS = {
  STREAM: 60_000,
  API: 10_000,
  BALANCE_POLL: 15_000,
} as const;
```

- [ ] **Step 3.2: `tests/e2e/helpers/guards.ts`**

```ts
import { TEST_PHONES } from './testData';

const FIXED_TEST_PHONES: readonly string[] = [
  TEST_PHONES.USER,
  TEST_PHONES.ADMIN,
];

// Паттерн для динамически создаваемых номеров в referral-тестах.
// Бэкенд применяет тот же паттерн к whitelist debug-эндпоинтов.
const TEMP_PHONE_PATTERN = /^790300\d{5}$/;

export function assertIsTestPhone(phone: string): void {
  if (FIXED_TEST_PHONES.includes(phone)) return;
  if (TEMP_PHONE_PATTERN.test(phone)) return;
  throw new Error(
    `SAFETY: only test phones allowed, got: ${phone}. ` +
    `Allowed: ${FIXED_TEST_PHONES.join(', ')} or pattern 790300XXXXX`,
  );
}

export function generateTempReferralPhone(): string {
  // 790300 + 5 случайных цифр. Диапазон ${10000, 99999}.
  const suffix = Math.floor(Math.random() * 90000) + 10000;
  return `790300${suffix}`;
}
```

- [ ] **Step 3.3: `tests/e2e/helpers/guards.test.ts` (unit-тест через Playwright)**

```ts
import { test, expect } from '@playwright/test';
import { assertIsTestPhone, generateTempReferralPhone } from './guards';

test.describe('guards', () => {
  test('assertIsTestPhone разрешает фиксированные номера', () => {
    expect(() => assertIsTestPhone('70000000000')).not.toThrow();
    expect(() => assertIsTestPhone('79030169187')).not.toThrow();
  });

  test('assertIsTestPhone разрешает pattern 790300XXXXX', () => {
    expect(() => assertIsTestPhone('79030012345')).not.toThrow();
    expect(() => assertIsTestPhone('79030099999')).not.toThrow();
  });

  test('assertIsTestPhone бросает на чужом номере', () => {
    expect(() => assertIsTestPhone('79991234567')).toThrow(/SAFETY/);
    expect(() => assertIsTestPhone('')).toThrow(/SAFETY/);
  });

  test('generateTempReferralPhone соответствует паттерну', () => {
    for (let i = 0; i < 10; i++) {
      const p = generateTempReferralPhone();
      expect(p).toMatch(/^790300\d{5}$/);
      expect(() => assertIsTestPhone(p)).not.toThrow();
    }
  });
});
```

> **Примечание:** `testDir` в Task 1 уже настроен на `./tests/e2e` с matcher `*.(test|spec).ts` — helper-тесты (`helpers/*.test.ts`) и flow-тесты (`flows/*.spec.ts`) подхватываются автоматически.

- [ ] **Step 3.4: Запустить guards-тесты**

```bash
cd ~/Downloads/spirits_front
pnpm exec playwright test tests/e2e/helpers/guards.test.ts --project=chromium-anon
```

> Если `globalSetup` ещё не создан и падает — временно закомментировать строку `globalSetup:` в конфиге. Вернём в Task 5.

Expected: 4 теста проходят (`4 passed`).

- [ ] **Step 3.5: Commit**

```bash
git add tests/e2e/helpers/ playwright.config.ts
git commit -m "test: helpers foundation для E2E — testData + guards + unit-тесты guards"
```

---

## Task 4: OTP helper + integration test

**Files:**
- Create: `spirits_front/tests/e2e/helpers/otp.ts`

- [ ] **Step 4.1: Написать `otp.ts`**

```ts
import { BASE_URL } from './testData';
import { assertIsTestPhone } from './guards';

/**
 * Получить последний SMS-код для тестового номера через debug-эндпоинт.
 * Работает только если на бэке DEBUG_SMS_CODES=true и номер в whitelist.
 *
 * @param phone — цифровая строка без +, например '70000000000'
 * @param opts.retries — сколько раз ретраить (код может ещё не успеть записаться в Redis)
 * @param opts.delayMs — пауза между попытками
 */
export async function fetchOtp(
  phone: string,
  opts: { retries?: number; delayMs?: number } = {},
): Promise<string> {
  assertIsTestPhone(phone);
  const retries = opts.retries ?? 5;
  const delayMs = opts.delayMs ?? 1000;

  for (let attempt = 1; attempt <= retries; attempt++) {
    const res = await fetch(`${BASE_URL}/webhook/debug/sms-code/${phone}`);
    if (res.ok) {
      const body = (await res.json()) as { code?: string };
      if (body.code) return body.code;
    } else if (res.status === 403) {
      throw new Error(`fetchOtp: phone ${phone} rejected by backend whitelist`);
    }
    if (attempt < retries) {
      await new Promise((r) => setTimeout(r, delayMs));
    }
  }

  throw new Error(
    `fetchOtp: no code for ${phone} after ${retries} attempts ` +
    `(backend DEBUG_SMS_CODES might be off, or SMS request wasn't sent yet)`,
  );
}
```

- [ ] **Step 4.2: Integration-тест `otp.test.ts`**

Создать `tests/e2e/helpers/otp.test.ts`:

```ts
import { test, expect } from '@playwright/test';
import { fetchOtp } from './otp';
import { BASE_URL, TEST_PHONES } from './testData';

test.describe('otp helper', () => {
  test('получает код для TEST_PHONES.USER после запроса SMS', async () => {
    // Инициируем отправку SMS (получение UUID webhook из .env — опционально;
    // здесь проверяем только что debug-эндпоинт доступен)
    // Первый вызов может не иметь кода, если SMS не запрашивали — допускаем.
    // Запросим SMS напрямую через API: нам нужно знать sms-webhook UUID.
    // Для простоты теста — просто вызовем fetchOtp и либо код есть, либо нет.

    try {
      const code = await fetchOtp(TEST_PHONES.USER, { retries: 1, delayMs: 0 });
      expect(code).toMatch(/^\d{4,6}$/);
    } catch (e) {
      // Если код ещё не генерился — это ок для интеграционной проверки.
      // Главное — fetchOtp не упал из-за guard/whitelist/404 на энвах.
      expect(String(e)).toMatch(/no code|DEBUG_SMS_CODES/i);
    }
  });

  test('бросает на не-тестовом телефоне', async () => {
    await expect(fetchOtp('79991234567')).rejects.toThrow(/SAFETY/);
  });
});
```

- [ ] **Step 4.3: Запустить тесты helper-ов**

```bash
pnpm exec playwright test tests/e2e/helpers --project=chromium-anon
```

Expected: все тесты проходят.

- [ ] **Step 4.4: Commit**

```bash
git add tests/e2e/helpers/otp.ts tests/e2e/helpers/otp.test.ts
git commit -m "test: otp helper + integration test для fetchOtp"
```

---

## Task 5: Login helper + globalSetup

**Files:**
- Create: `spirits_front/tests/e2e/helpers/login.ts`
- Create: `spirits_front/tests/e2e/global-setup.ts`

Цель: один раз за прогон тестов залогиниться под test-user и test-admin, записать `storageState` в `.auth/*.json`. Последующие тесты стартуют уже авторизованными.

- [ ] **Step 5.1: Изучить API логина в коде фронта**

Прочитать `src/services/authService.ts` чтобы понять:
- URL отправки SMS (обычно `/webhook/{uuid}/sms/{phone}`)
- URL проверки кода
- Формат ответа (структура `AuthResponse` в `src/types/auth.ts`)
- Какие ключи пишутся в `localStorage`

Записать URL-ы в комментарии `login.ts`.

- [ ] **Step 5.2: Написать `login.ts`**

```ts
import { BASE_URL, TEST_PHONES } from './testData';
import { assertIsTestPhone } from './guards';
import { fetchOtp } from './otp';

export interface LoginResult {
  accessToken: string;
  refreshToken: string;
  userData: unknown;
}

// URL отправки SMS берётся из src/services/authService.ts — это захардкоженный
// UUID-webhook. Если он поменяется, обновить здесь.
const SMS_WEBHOOK_UUID = '898c938d-f094-455c-86af-969617e62f7a';
const CHECK_WEBHOOK_UUID = 'a376a8ed-3bf7-4f23-aaa5-236eea72871b';

export async function loginViaApi(phone: string): Promise<LoginResult> {
  assertIsTestPhone(phone);

  // 1. Запросить SMS-код
  const smsRes = await fetch(`${BASE_URL}/webhook/${SMS_WEBHOOK_UUID}/sms/${phone}`);
  if (!smsRes.ok) {
    throw new Error(`SMS request failed: ${smsRes.status} ${await smsRes.text()}`);
  }

  // 2. Получить код из debug-эндпоинта
  const code = await fetchOtp(phone, { retries: 10, delayMs: 1000 });

  // 3. Проверить код → получить JWT
  const checkRes = await fetch(
    `${BASE_URL}/webhook/${CHECK_WEBHOOK_UUID}/check-code/${phone}/${code}`,
  );
  if (!checkRes.ok) {
    throw new Error(`check-code failed: ${checkRes.status} ${await checkRes.text()}`);
  }
  const body = await checkRes.json();

  // Структура ответа зафиксирована в src/types/auth.ts (AuthResponse).
  // Ожидаем поля access_token, refresh_token и user / userData.
  const accessToken = body.access_token ?? body.jwt_access_token ?? body.accessToken;
  const refreshToken = body.refresh_token ?? body.jwt_refresh_token ?? body.refreshToken;
  const userData = body.user ?? body.userData ?? body;

  if (!accessToken || !refreshToken) {
    throw new Error(`login: missing tokens in response: ${JSON.stringify(body)}`);
  }

  return { accessToken, refreshToken, userData };
}
```

> **Important:** в Step 5.1 обязательно сверить имена полей в фактическом ответе бэка (`access_token` vs `accessToken` и т.д.) и подправить `loginViaApi` так, чтобы не было fallback-цепочки — оставить ровно одно ожидаемое имя. Fallback-цепочка выше — временная, до сверки.

- [ ] **Step 5.3: Написать `global-setup.ts`**

```ts
import { FullConfig, chromium } from '@playwright/test';
import * as fs from 'fs';
import * as path from 'path';
import { loginViaApi } from './helpers/login';
import { TEST_PHONES, BASE_URL, AUTH_STATE_PATH } from './helpers/testData';

export default async function globalSetup(_config: FullConfig) {
  await saveAuthState(TEST_PHONES.USER, AUTH_STATE_PATH.USER);
  await saveAuthState(TEST_PHONES.ADMIN, AUTH_STATE_PATH.ADMIN);
}

async function saveAuthState(phone: string, outPath: string): Promise<void> {
  const { accessToken, refreshToken, userData } = await loginViaApi(phone);

  // Создаём браузерный контекст, инжектим localStorage, сохраняем storageState.
  const browser = await chromium.launch();
  const ctx = await browser.newContext();
  const page = await ctx.newPage();
  await page.goto(BASE_URL);

  await page.evaluate(
    ({ accessToken, refreshToken, userData }) => {
      localStorage.setItem('jwt_access_token', accessToken);
      localStorage.setItem('jwt_refresh_token', refreshToken);
      localStorage.setItem('authToken', accessToken); // legacy-совместимость
      localStorage.setItem('userData', JSON.stringify(userData));
    },
    { accessToken, refreshToken, userData },
  );

  fs.mkdirSync(path.dirname(outPath), { recursive: true });
  await ctx.storageState({ path: outPath });
  await browser.close();

  console.log(`[globalSetup] saved auth state for ${phone} → ${outPath}`);
}
```

- [ ] **Step 5.4: Вернуть `globalSetup` в `playwright.config.ts`**

Раскомментировать (если в Task 3 комментировали):

```ts
globalSetup: './tests/e2e/global-setup.ts',
```

- [ ] **Step 5.5: Запустить globalSetup через dry-run**

```bash
pnpm exec playwright test tests/e2e/helpers --project=chromium-anon --reporter=list
```

Expected:
- В консоли: `[globalSetup] saved auth state for 70000000000 → ./tests/e2e/.auth/test-user.json`
- И аналогично для admin.
- Файлы `.auth/test-user.json` и `.auth/test-admin.json` созданы.
- Helper-тесты проходят.

- [ ] **Step 5.6: Проверить содержимое storageState**

```bash
cat tests/e2e/.auth/test-user.json | python3 -m json.tool | head -40
```

Expected: JSON с полями `cookies`, `origins` → для `https://my.linkeon.io` → `localStorage` с `jwt_access_token`, `jwt_refresh_token`, `userData`.

- [ ] **Step 5.7: Commit**

```bash
git add tests/e2e/helpers/login.ts tests/e2e/global-setup.ts playwright.config.ts
git commit -m "test: login helper + globalSetup — кеширование auth state для двух ролей"
```

---

## Task 6: Cleanup helpers

**Files:**
- Create: `spirits_front/tests/e2e/helpers/cleanup.ts`

- [ ] **Step 6.1: Написать `cleanup.ts`**

```ts
import { APIRequestContext, Page } from '@playwright/test';
import { BASE_URL, TEST_PHONES } from './testData';
import { assertIsTestPhone } from './guards';

/**
 * Извлечь JWT access token из страницы (из localStorage в текущем контексте).
 */
async function getAccessToken(page: Page): Promise<string> {
  const token = await page.evaluate(() => localStorage.getItem('jwt_access_token'));
  if (!token) throw new Error('cleanup: no jwt_access_token in page localStorage');
  return token;
}

/**
 * Очистить историю чата с конкретным ассистентом.
 */
export async function clearChatHistory(page: Page, assistantId: string): Promise<void> {
  const token = await getAccessToken(page);
  const res = await fetch(
    `${BASE_URL}/webhook/chat/history?assistantId=${encodeURIComponent(assistantId)}`,
    { method: 'DELETE', headers: { Authorization: `Bearer ${token}` } },
  );
  if (!res.ok) {
    throw new Error(`clearChatHistory failed: ${res.status} ${await res.text()}`);
  }
}

/**
 * Обновить произвольные поля профиля.
 */
export async function updateProfile(
  page: Page,
  payload: Record<string, unknown>,
): Promise<void> {
  const token = await getAccessToken(page);
  const res = await fetch(`${BASE_URL}/webhook/profile-update`, {
    method: 'POST',
    headers: {
      Authorization: `Bearer ${token}`,
      'Content-Type': 'application/json',
    },
    body: JSON.stringify(payload),
  });
  if (!res.ok) {
    throw new Error(`updateProfile failed: ${res.status} ${await res.text()}`);
  }
}

/**
 * Изменить баланс токенов через debug-эндпоинт (может быть отрицательным).
 */
export async function resetTokens(phone: string, delta: number): Promise<void> {
  assertIsTestPhone(phone);
  const res = await fetch(
    `${BASE_URL}/webhook/debug/add-tokens/${phone}/${delta}`,
    { method: 'POST' },
  );
  if (!res.ok) {
    throw new Error(`resetTokens failed: ${res.status} ${await res.text()}`);
  }
}

/**
 * Удалить купон через admin API.
 * Требует admin-storageState (page должен быть из admin-project).
 */
export async function deleteCoupon(page: Page, code: string): Promise<void> {
  const token = await getAccessToken(page);
  const res = await fetch(`${BASE_URL}/webhook/admin/coupons`, {
    method: 'POST',
    headers: {
      Authorization: `Bearer ${token}`,
      'Content-Type': 'application/json',
    },
    body: JSON.stringify({ action: 'delete', code }),
  });
  if (!res.ok) {
    throw new Error(`deleteCoupon failed: ${res.status} ${await res.text()}`);
  }
}

/**
 * Создать купон через admin API. Возвращает созданный объект.
 */
export async function createCoupon(
  page: Page,
  params: { code: string; tokens: number },
): Promise<unknown> {
  const token = await getAccessToken(page);
  const res = await fetch(`${BASE_URL}/webhook/admin/coupons`, {
    method: 'POST',
    headers: {
      Authorization: `Bearer ${token}`,
      'Content-Type': 'application/json',
    },
    body: JSON.stringify({ action: 'create', ...params }),
  });
  if (!res.ok) {
    throw new Error(`createCoupon failed: ${res.status} ${await res.text()}`);
  }
  return res.json();
}
```

- [ ] **Step 6.2: Commit**

```bash
git add tests/e2e/helpers/cleanup.ts
git commit -m "test: cleanup helpers (chat history, profile, tokens, coupons)"
```

---

## Task 7: Mock YooKassa helper

**Files:**
- Create: `spirits_front/tests/e2e/helpers/mockYookassa.ts`

- [ ] **Step 7.1: Написать `mockYookassa.ts`**

```ts
import { Page } from '@playwright/test';

/**
 * Перехватывает запрос на создание YooKassa-платежа и возвращает фейковый
 * confirmation_url, ведущий на страницу успеха того же фронта.
 */
export async function mockYookassaCheckout(page: Page): Promise<void> {
  await page.route('**/webhook/yookassa/create-payment', async (route) => {
    const paymentId = `test-pw-${Date.now()}`;
    await route.fulfill({
      status: 200,
      contentType: 'application/json',
      body: JSON.stringify({
        payment_id: paymentId,
        confirmation_url: `/payment/success?fake=1&payment_id=${paymentId}`,
        status: 'pending',
      }),
    });
  });
}

/**
 * Перехватывает polling payment-status и возвращает "succeeded".
 */
export async function mockYookassaPaymentStatus(page: Page): Promise<void> {
  await page.route('**/webhook/payment-status*', async (route) => {
    await route.fulfill({
      status: 200,
      contentType: 'application/json',
      body: JSON.stringify({ status: 'succeeded' }),
    });
  });
}
```

- [ ] **Step 7.2: Commit**

```bash
git add tests/e2e/helpers/mockYookassa.ts
git commit -m "test: mockYookassa helper — перехват create-payment и payment-status"
```

---

## Task 8: Flow 01 — onboarding

**Files:**
- Modify: `src/components/onboarding/PhoneInput.tsx` — добавить `data-testid`
- Modify: `src/components/onboarding/OTPInput.tsx` — `data-testid`
- Modify: `src/components/onboarding/LegalModal.tsx` — `data-testid` (если есть)
- Create: `tests/e2e/flows/01-onboarding.spec.ts`

- [ ] **Step 8.1: Прочитать исходники onboarding-компонентов**

Открыть и прочитать:
- `src/components/onboarding/PhoneInput.tsx`
- `src/components/onboarding/OTPInput.tsx`
- `src/components/onboarding/LegalModal.tsx`

Определить: корневой элемент формы, name/имя класса у `<input type="tel">`, submit-кнопка (обычно та, где иконка `ArrowRight`), OTP-инпут (один общий или 6 отдельных). От этого зависит, как подставить testid в Step 8.2-8.4.

- [ ] **Step 8.2: Добавить data-testid в PhoneInput.tsx**

На корневой `<form>` или внешний `<div>`: `data-testid="onboarding-root"`.
На `<input type="tel">`: `data-testid="phone-input"`.
На submit-кнопку (обычно та, где `ArrowRight` иконка): `data-testid="phone-submit-btn"`.

- [ ] **Step 8.3: Добавить data-testid в OTPInput.tsx**

Если OTP — 6 отдельных инпутов: каждому `data-testid={\`otp-input-${index}\`}`.
Если один скрытый с auto-focus на следующий — `data-testid="otp-input"` на корень.
Submit-кнопка (если есть): `data-testid="otp-submit-btn"`.

- [ ] **Step 8.4: Добавить data-testid в LegalModal.tsx**

Кнопка согласия: `data-testid="legal-accept-btn"`.

- [ ] **Step 8.5: Написать `tests/e2e/flows/01-onboarding.spec.ts`**

```ts
import { test, expect } from '@playwright/test';
import { fetchOtp } from '../helpers/otp';
import { TEST_PHONES, BASE_URL, TIMEOUTS } from '../helpers/testData';

// Onboarding стартует с пустого состояния — используем anon-project.
test.use({ storageState: { cookies: [], origins: [] } });

test.describe.configure({ mode: 'serial' });

test('01 onboarding: phone → SMS → OTP → /chat', async ({ page }) => {
  test.info().annotations.push({ type: 'project', description: 'chromium-anon' });

  await page.goto(BASE_URL);

  // Убедились, что мы на экране онбординга.
  await expect(page.getByTestId('onboarding-root')).toBeVisible({ timeout: TIMEOUTS.API });

  // Вводим телефон.
  const phoneInput = page.getByTestId('phone-input');
  await phoneInput.click();
  await phoneInput.fill(TEST_PHONES.USER);

  // Ждём, пока фронт отправит SMS-запрос, и одновременно кликаем submit.
  await Promise.all([
    page.waitForResponse((r) =>
      r.url().includes('/webhook/') && r.url().includes('/sms/') && r.status() < 400,
    ),
    page.getByTestId('phone-submit-btn').click(),
  ]);

  // Дожидаемся code из debug-эндпоинта (с ретраями — Redis может не успеть).
  const code = await fetchOtp(TEST_PHONES.USER, { retries: 10, delayMs: 1000 });

  // Вводим OTP. Проверяем оба варианта: 6 отдельных инпутов ИЛИ один.
  const singleOtp = page.getByTestId('otp-input');
  if (await singleOtp.count()) {
    await singleOtp.fill(code);
  } else {
    for (let i = 0; i < code.length; i++) {
      await page.getByTestId(`otp-input-${i}`).fill(code[i]);
    }
  }

  // Если есть явная submit-кнопка OTP — жмём. Если OTP submit-ится автоматически — пропускаем.
  const otpSubmit = page.getByTestId('otp-submit-btn');
  if (await otpSubmit.count()) {
    await otpSubmit.click();
  }

  // Legal modal — если показан, принимаем.
  const legalBtn = page.getByTestId('legal-accept-btn');
  if (await legalBtn.count() && await legalBtn.isVisible().catch(() => false)) {
    await legalBtn.click();
  }

  // Редирект на /chat.
  await page.waitForURL(/\/chat/, { timeout: TIMEOUTS.STREAM });
  expect(page.url()).toContain('/chat');

  // На /chat отрисовался AssistantSelection или приветственное сообщение.
  await expect(page.getByTestId('chat-root')).toBeVisible({ timeout: TIMEOUTS.API });
});
```

- [ ] **Step 8.6: Запустить flow 01**

```bash
pnpm exec playwright test tests/e2e/flows/01-onboarding.spec.ts --project=chromium-anon --headed
```

Expected: тест проходит, видим в headed-режиме как бот проходит onboarding.

Если тест падает на `phone-input` — проверить, правильно ли добавили `data-testid` (перезапустить `pnpm dev` или дождаться пересборки, но так как мы ходим на prod — нужно сделать `pnpm build && rsync`. **Это важный момент:** изменения `data-testid` должны попасть в prod-сборку, иначе Playwright не увидит их.

- [ ] **Step 8.7: Задеплоить фронт на prod**

```bash
cd ~/Downloads/spirits_front
echo "VITE_BACKEND_URL=https://my.linkeon.io" > .env
pnpm build
rsync -az --delete -e "ssh -p 60322" dist/ dvolkov@82.202.197.230:/var/www/spirits/dist/
```

Повторить Step 8.6. Тест должен проходить.

- [ ] **Step 8.8: Commit**

```bash
git add src/components/onboarding/ tests/e2e/flows/01-onboarding.spec.ts
git commit -m "test: E2E flow 01 — onboarding (phone → SMS → OTP → /chat)"
```

---

## Task 9: Flow 02 — chat с ассистентом

**Files:**
- Modify: `src/components/chat/ChatInterface.tsx` — `data-testid`
- Modify: `src/components/chat/AssistantSelection.tsx` — `data-testid`
- Create: `tests/e2e/flows/02-chat.spec.ts`

- [ ] **Step 9.1: Добавить data-testid в ChatInterface.tsx**

- Корень компонента: `data-testid="chat-root"`
- Input сообщения: `data-testid="chat-input"`
- Кнопка отправки: `data-testid="send-message-btn"`
- Контейнер сообщения ассистента: `data-testid="assistant-message"` (на каждом сообщении, или только на последнем — первый вариант удобнее для `last()`)
- Контейнер сообщения пользователя: `data-testid="user-message"`
- Элемент с балансом токенов: `data-testid="token-balance"`

- [ ] **Step 9.2: Добавить data-testid в AssistantSelection.tsx**

- Корень: `data-testid="assistant-selection"`
- Каждый элемент ассистента: `data-testid={\`assistant-item-${assistant.id}\`}`
- Имя текущего ассистента (заголовок чата): `data-testid="active-assistant-name"`

- [ ] **Step 9.3: Написать `tests/e2e/flows/02-chat.spec.ts`**

```ts
import { test, expect } from '@playwright/test';
import { clearChatHistory } from '../helpers/cleanup';
import { TIMEOUTS } from '../helpers/testData';

test.describe('02 chat', () => {
  test('выбор ассистента, отправка сообщения, стриминг, списание токенов', async ({ page }) => {
    await page.goto('/chat');
    await expect(page.getByTestId('chat-root')).toBeVisible();

    // Берём первого ассистента.
    const firstAssistant = page.getByTestId('assistant-selection')
      .locator('[data-testid^="assistant-item-"]').first();
    const assistantId = (await firstAssistant.getAttribute('data-testid'))!
      .replace('assistant-item-', '');
    await firstAssistant.click();

    // Записываем текущий баланс.
    const balanceText = await page.getByTestId('token-balance').innerText();
    const balanceBefore = parseInt(balanceText.replace(/\D/g, ''), 10);
    expect(balanceBefore).toBeGreaterThan(0);

    // Отправляем сообщение.
    await page.getByTestId('chat-input').fill('Привет, ты работаешь? Ответь одним словом.');
    await page.getByTestId('send-message-btn').click();

    // Дожидаемся появления ответа ассистента.
    const lastAssistantMsg = page.getByTestId('assistant-message').last();
    await expect(lastAssistantMsg).toBeVisible({ timeout: TIMEOUTS.STREAM });

    // Ждём, чтобы стрим остановился — проверяем, что текст не пустой.
    await expect(async () => {
      const text = await lastAssistantMsg.innerText();
      expect(text.trim().length).toBeGreaterThan(1);
    }).toPass({ timeout: TIMEOUTS.STREAM });

    // Баланс должен уменьшиться в течение TIMEOUTS.BALANCE_POLL.
    await expect(async () => {
      const t = await page.getByTestId('token-balance').innerText();
      const now = parseInt(t.replace(/\D/g, ''), 10);
      expect(now).toBeLessThan(balanceBefore);
    }).toPass({ timeout: TIMEOUTS.BALANCE_POLL });

    // Cleanup
    await clearChatHistory(page, assistantId);
  });
});

test.use({ storageState: './tests/e2e/.auth/test-user.json' });
```

- [ ] **Step 9.4: Передеплой фронта на prod**

```bash
pnpm build
rsync -az --delete -e "ssh -p 60322" dist/ dvolkov@82.202.197.230:/var/www/spirits/dist/
```

- [ ] **Step 9.5: Запустить flow 02**

```bash
pnpm exec playwright test tests/e2e/flows/02-chat.spec.ts --project=chromium-user
```

Expected: тест проходит, в отчёте видим списание токенов.

- [ ] **Step 9.6: Commit**

```bash
git add src/components/chat/ tests/e2e/flows/02-chat.spec.ts
git commit -m "test: E2E flow 02 — чат с ассистентом, стриминг, списание токенов"
```

---

## Task 10: Flow 03 — переключение ассистента

**Files:**
- Create: `tests/e2e/flows/03-assistant-switch.spec.ts`

(data-testid для чата уже добавлены в Task 9)

- [ ] **Step 10.1: Написать `03-assistant-switch.spec.ts`**

```ts
import { test, expect } from '@playwright/test';
import { clearChatHistory } from '../helpers/cleanup';
import { TIMEOUTS } from '../helpers/testData';

test.use({ storageState: './tests/e2e/.auth/test-user.json' });

test('03 assistant switch: история изолирована между ассистентами', async ({ page }) => {
  await page.goto('/chat');

  const items = page.getByTestId('assistant-selection').locator('[data-testid^="assistant-item-"]');
  const count = await items.count();
  expect(count).toBeGreaterThanOrEqual(2);

  const idA = (await items.nth(0).getAttribute('data-testid'))!.replace('assistant-item-', '');
  const idB = (await items.nth(1).getAttribute('data-testid'))!.replace('assistant-item-', '');

  // Чистим историю перед началом, чтобы тест был идемпотентным.
  await clearChatHistory(page, idA);
  await clearChatHistory(page, idB);
  await page.reload();

  // Ассистент A — отправляем сообщение.
  await items.nth(0).click();
  await page.getByTestId('chat-input').fill('Сообщение A — тестовое');
  await page.getByTestId('send-message-btn').click();
  await expect(page.getByTestId('assistant-message').last())
    .toBeVisible({ timeout: TIMEOUTS.STREAM });

  // Переключаем на B.
  await items.nth(1).click();

  // У B не должно быть "Сообщение A".
  await expect(page.locator('body')).not.toContainText('Сообщение A — тестовое', {
    timeout: TIMEOUTS.API,
  });

  // Отправляем B.
  await page.getByTestId('chat-input').fill('Сообщение B — тестовое');
  await page.getByTestId('send-message-btn').click();
  await expect(page.getByTestId('assistant-message').last())
    .toBeVisible({ timeout: TIMEOUTS.STREAM });

  // Возвращаемся на A — "Сообщение A" должно быть в истории.
  await items.nth(0).click();
  await expect(page.locator('body')).toContainText('Сообщение A — тестовое', {
    timeout: TIMEOUTS.API,
  });

  // Cleanup.
  await clearChatHistory(page, idA);
  await clearChatHistory(page, idB);
});
```

- [ ] **Step 10.2: Запустить**

```bash
pnpm exec playwright test tests/e2e/flows/03-assistant-switch.spec.ts --project=chromium-user
```

- [ ] **Step 10.3: Commit**

```bash
git add tests/e2e/flows/03-assistant-switch.spec.ts
git commit -m "test: E2E flow 03 — переключение ассистента, изоляция истории"
```

---

## Task 11: Flow 04 — профиль

**Files:**
- Modify: `src/components/profile/ProfileView.tsx` — `data-testid`
- Create: `tests/e2e/flows/04-profile.spec.ts`

- [ ] **Step 11.1: Добавить data-testid в ProfileView.tsx**

Прочитать файл, идентифицировать:
- Корень: `data-testid="profile-root"`
- Секции Neo4j-данных (values, beliefs, desires, intents, interests, skills): `data-testid={\`profile-section-${key}\`}`
- Инпут firstName (в режиме редактирования): `data-testid="profile-field-firstName"`
- Кнопка "Редактировать": `data-testid="profile-edit-btn"`
- Кнопка "Сохранить": `data-testid="profile-save-btn"`

- [ ] **Step 11.2: Написать `04-profile.spec.ts`**

```ts
import { test, expect } from '@playwright/test';
import { updateProfile } from '../helpers/cleanup';

test.use({ storageState: './tests/e2e/.auth/test-user.json' });

test('04 profile: edit firstName, reload, verify persisted', async ({ page }) => {
  await page.goto('/profile');
  await expect(page.getByTestId('profile-root')).toBeVisible();

  // Neo4j-секции отрисованы (пусть даже пустые).
  await expect(page.getByTestId('profile-section-values')).toBeVisible();

  // Запоминаем исходный firstName через API (чтобы потом откатить).
  // Фронт хранит userData в localStorage.
  const originalFirstName = (await page.evaluate(() => {
    const raw = localStorage.getItem('userData');
    if (!raw) return '';
    try { return JSON.parse(raw).firstName ?? ''; } catch { return ''; }
  })) as string;

  // Режим редактирования.
  await page.getByTestId('profile-edit-btn').click();

  const unique = `E2E-${Date.now()}`;
  const input = page.getByTestId('profile-field-firstName');
  await input.fill(unique);
  await page.getByTestId('profile-save-btn').click();

  // Перезагружаем, проверяем что значение подгрузилось.
  await page.reload();
  await expect(page.getByTestId('profile-root')).toContainText(unique);

  // Cleanup.
  try {
    await updateProfile(page, { firstName: originalFirstName });
  } catch (e) {
    console.warn('cleanup updateProfile failed:', e);
  }
});
```

- [ ] **Step 11.3: Передеплой фронта + запуск**

```bash
pnpm build && rsync -az --delete -e "ssh -p 60322" dist/ dvolkov@82.202.197.230:/var/www/spirits/dist/
pnpm exec playwright test tests/e2e/flows/04-profile.spec.ts --project=chromium-user
```

- [ ] **Step 11.4: Commit**

```bash
git add src/components/profile/ProfileView.tsx tests/e2e/flows/04-profile.spec.ts
git commit -m "test: E2E flow 04 — редактирование firstName, проверка персистентности"
```

---

## Task 12: Flow 05 — покупка токенов через мок-YooKassa

**Files:**
- Modify: `src/components/tokens/TokenPackages.tsx` — `data-testid`
- Create: `tests/e2e/flows/05-tokens-purchase.spec.ts`

- [ ] **Step 12.1: Добавить data-testid в TokenPackages.tsx**

- Корень: `data-testid="tokens-root"`
- Каждый пакет: `data-testid="token-package-{basic|extended|pro}"` (в зависимости от id в компоненте)
- Элемент с балансом на этой странице: переиспользовать `data-testid="token-balance"` если уже есть в layout

- [ ] **Step 12.2: Написать `05-tokens-purchase.spec.ts`**

```ts
import { test, expect } from '@playwright/test';
import { mockYookassaCheckout, mockYookassaPaymentStatus } from '../helpers/mockYookassa';
import { resetTokens } from '../helpers/cleanup';
import { TEST_PHONES, TIMEOUTS } from '../helpers/testData';

test.use({ storageState: './tests/e2e/.auth/test-user.json' });

const PACKAGE_TOKENS = 50_000;

test('05 tokens purchase: mock YooKassa → payment/success → balance +50k', async ({ page }) => {
  await mockYookassaCheckout(page);
  await mockYookassaPaymentStatus(page);

  await page.goto('/tokens');
  await expect(page.getByTestId('tokens-root')).toBeVisible();

  const before = await page.getByTestId('token-balance').innerText()
    .then(t => parseInt(t.replace(/\D/g, ''), 10));

  await page.getByTestId('token-package-basic').click();

  // Фронт сделал запрос на /yookassa/create-payment, получил confirmation_url,
  // редиректнул на /payment/success?fake=1.
  await page.waitForURL(/\/payment\/success/, { timeout: TIMEOUTS.API });

  // Имитируем колбэк YooKassa: дёргаем debug-эндпоинт add-tokens.
  await resetTokens(TEST_PHONES.USER, PACKAGE_TOKENS);

  // Возвращаемся на /chat, ждём обновления баланса через polling.
  await page.goto('/chat');
  await expect(async () => {
    const t = await page.getByTestId('token-balance').innerText();
    const now = parseInt(t.replace(/\D/g, ''), 10);
    expect(now).toBeGreaterThanOrEqual(before + PACKAGE_TOKENS);
  }).toPass({ timeout: TIMEOUTS.BALANCE_POLL });

  // Cleanup: откатываем бонус.
  await resetTokens(TEST_PHONES.USER, -PACKAGE_TOKENS);
});
```

- [ ] **Step 12.3: Передеплой + запуск**

```bash
pnpm build && rsync -az --delete -e "ssh -p 60322" dist/ dvolkov@82.202.197.230:/var/www/spirits/dist/
pnpm exec playwright test tests/e2e/flows/05-tokens-purchase.spec.ts --project=chromium-user
```

- [ ] **Step 12.4: Commit**

```bash
git add src/components/tokens/TokenPackages.tsx tests/e2e/flows/05-tokens-purchase.spec.ts
git commit -m "test: E2E flow 05 — покупка токенов через mock-YooKassa + debug add-tokens"
```

---

## Task 13: Flow 06 — купон

**Files:**
- Modify: `src/components/tokens/CouponInput.tsx` — `data-testid`
- Create: `tests/e2e/flows/06-coupon.spec.ts`

- [ ] **Step 13.1: Добавить data-testid в CouponInput.tsx**

- Корень: `data-testid="coupon-root"` (или пропустить, если в родителе уже есть `tokens-root`)
- Input кода: `data-testid="coupon-input"`
- Submit: `data-testid="coupon-submit-btn"`
- Элемент success-сообщения: `data-testid="coupon-success"`
- Элемент error-сообщения: `data-testid="coupon-error"`

- [ ] **Step 13.2: Написать `06-coupon.spec.ts`**

Для этого теста нужен admin-JWT, чтобы создать тестовый купон перед user-тестом. Идём через APIRequestContext напрямую:

```ts
import { test, expect, request } from '@playwright/test';
import { BASE_URL, TEST_PHONES, TIMEOUTS } from '../helpers/testData';
import { resetTokens } from '../helpers/cleanup';
import * as fs from 'fs';

test.use({ storageState: './tests/e2e/.auth/test-user.json' });

const COUPON_TOKENS = 1000;

async function getAdminToken(): Promise<string> {
  // Читаем admin auth state и извлекаем access_token.
  const raw = fs.readFileSync('./tests/e2e/.auth/test-admin.json', 'utf-8');
  const state = JSON.parse(raw);
  const origin = state.origins.find((o: any) => o.origin === BASE_URL);
  const tokenEntry = origin.localStorage.find((e: any) => e.name === 'jwt_access_token');
  return tokenEntry.value;
}

test('06 coupon: redeem валидного купона → баланс + COUPON_TOKENS', async ({ page }) => {
  const couponCode = `E2E-PW-${Date.now()}`;
  const adminToken = await getAdminToken();
  const api = await request.newContext({ baseURL: BASE_URL });

  // 1. Admin создаёт купон.
  const create = await api.post('/webhook/admin/coupons', {
    headers: { Authorization: `Bearer ${adminToken}`, 'Content-Type': 'application/json' },
    data: { action: 'create', code: couponCode, tokens: COUPON_TOKENS },
  });
  expect(create.ok()).toBeTruthy();

  try {
    // 2. User активирует.
    await page.goto('/tokens');

    const balanceBefore = await page.getByTestId('token-balance').innerText()
      .then(t => parseInt(t.replace(/\D/g, ''), 10));

    await page.getByTestId('coupon-input').fill(couponCode);
    await page.getByTestId('coupon-submit-btn').click();

    await expect(page.getByTestId('coupon-success')).toBeVisible({ timeout: TIMEOUTS.API });

    await expect(async () => {
      const t = await page.getByTestId('token-balance').innerText();
      const now = parseInt(t.replace(/\D/g, ''), 10);
      expect(now).toBeGreaterThanOrEqual(balanceBefore + COUPON_TOKENS);
    }).toPass({ timeout: TIMEOUTS.BALANCE_POLL });
  } finally {
    // 3. Cleanup: удаляем купон + откатываем токены.
    await api.post('/webhook/admin/coupons', {
      headers: { Authorization: `Bearer ${adminToken}`, 'Content-Type': 'application/json' },
      data: { action: 'delete', code: couponCode },
    });
    await resetTokens(TEST_PHONES.USER, -COUPON_TOKENS);
  }
});
```

- [ ] **Step 13.3: Передеплой + запуск**

```bash
pnpm build && rsync -az --delete -e "ssh -p 60322" dist/ dvolkov@82.202.197.230:/var/www/spirits/dist/
pnpm exec playwright test tests/e2e/flows/06-coupon.spec.ts --project=chromium-user
```

- [ ] **Step 13.4: Commit**

```bash
git add src/components/tokens/CouponInput.tsx tests/e2e/flows/06-coupon.spec.ts
git commit -m "test: E2E flow 06 — создание и активация купона"
```

---

## Task 14: Flow 07 — search + compatibility

**Files:**
- Modify: `src/components/search/SearchInterface.tsx` — `data-testid`
- Modify: `src/components/search/CompatibilityInterface.tsx` — `data-testid`
- Modify: `src/components/search/UserProfileModal.tsx` — `data-testid`
- Create: `tests/e2e/flows/07-search-compatibility.spec.ts`

- [ ] **Step 14.1: Добавить data-testid**

**SearchInterface.tsx:**
- Корень: `data-testid="search-root"`
- Input запроса: `data-testid="search-input"`
- Submit: `data-testid="search-submit-btn"` (если есть)
- Результаты: `data-testid={\`search-result-${index}\`}` на каждом

**UserProfileModal.tsx:**
- Корень модала: `data-testid="user-profile-modal"`
- Кнопка "Проверить совместимость": `data-testid="check-compatibility-btn"`

**CompatibilityInterface.tsx:**
- Корень: `data-testid="compatibility-root"`
- Контейнер результата-markdown: `data-testid="compatibility-result"`

- [ ] **Step 14.2: Написать `07-search-compatibility.spec.ts`**

```ts
import { test, expect } from '@playwright/test';
import { TIMEOUTS } from '../helpers/testData';

test.use({ storageState: './tests/e2e/.auth/test-user.json' });

test('07 search + compatibility', async ({ page }) => {
  await page.goto('/search');
  await expect(page.getByTestId('search-root')).toBeVisible();

  await page.getByTestId('search-input').fill('единомышленник');
  const submit = page.getByTestId('search-submit-btn');
  if (await submit.count()) await submit.click();
  else await page.getByTestId('search-input').press('Enter');

  // Ждём хотя бы один результат.
  await expect(page.getByTestId('search-result-0')).toBeVisible({ timeout: TIMEOUTS.STREAM });

  // Открываем первый результат.
  await page.getByTestId('search-result-0').click();
  await expect(page.getByTestId('user-profile-modal')).toBeVisible();

  // Проверяем совместимость.
  await page.getByTestId('check-compatibility-btn').click();

  await page.waitForURL(/\/compatibility/, { timeout: TIMEOUTS.API });
  await expect(page.getByTestId('compatibility-root')).toBeVisible();

  // Ждём заполнения результата.
  await expect(async () => {
    const text = await page.getByTestId('compatibility-result').innerText();
    expect(text.trim().length).toBeGreaterThan(20);
  }).toPass({ timeout: TIMEOUTS.STREAM });
});
```

- [ ] **Step 14.3: Передеплой + запуск**

```bash
pnpm build && rsync -az --delete -e "ssh -p 60322" dist/ dvolkov@82.202.197.230:/var/www/spirits/dist/
pnpm exec playwright test tests/e2e/flows/07-search-compatibility.spec.ts --project=chromium-user
```

- [ ] **Step 14.4: Commit**

```bash
git add src/components/search/ tests/e2e/flows/07-search-compatibility.spec.ts
git commit -m "test: E2E flow 07 — поиск людей и анализ совместимости"
```

---

## Task 15: Flow 08 — админка

**Files:**
- Modify: `src/components/admin/AdminAssistantsView.tsx`, `AdminCouponsView.tsx`, `AdminReferralsView.tsx` — `data-testid`
- Create: `tests/e2e/flows/08-admin.spec.ts`

- [ ] **Step 15.1: Добавить data-testid**

Найти в `src/pages/AdminPage.tsx` (или где-то в админ-layout) навигацию между вкладками и добавить:
- `data-testid="admin-root"`
- `data-testid="admin-tab-assistants"`, `"admin-tab-coupons"`, `"admin-tab-referral"` на каждой вкладке-кнопке
- `data-testid="admin-panel-assistants"`, etc. — на контейнере контента вкладки

В `AdminAssistantsView.tsx`:
- `data-testid="admin-assistants-list"` на корне списка

В `AdminCouponsView.tsx`:
- `data-testid="admin-coupons-list"`
- `data-testid="admin-coupons-create-btn"` на кнопке создания

В `AdminReferralsView.tsx`:
- `data-testid="admin-referral-summary"`
- `data-testid="admin-referral-leaders"`

- [ ] **Step 15.2: Написать `08-admin.spec.ts`**

```ts
import { test, expect } from '@playwright/test';
import { TIMEOUTS } from '../helpers/testData';

test.use({ storageState: './tests/e2e/.auth/test-admin.json' });

test('08 admin: три вкладки с данными', async ({ page }) => {
  await page.goto('/admin');
  await expect(page.getByTestId('admin-root')).toBeVisible();

  // Assistants
  await page.getByTestId('admin-tab-assistants').click();
  await expect(page.getByTestId('admin-assistants-list')).toBeVisible({ timeout: TIMEOUTS.API });
  const assistantsCount = await page.getByTestId('admin-assistants-list')
    .locator(':scope > *').count();
  expect(assistantsCount).toBeGreaterThan(0);

  // Coupons
  await page.getByTestId('admin-tab-coupons').click();
  await expect(page.getByTestId('admin-coupons-list')).toBeVisible({ timeout: TIMEOUTS.API });

  // Referral
  await page.getByTestId('admin-tab-referral').click();
  await expect(page.getByTestId('admin-referral-summary')).toBeVisible({ timeout: TIMEOUTS.API });
  await expect(page.getByTestId('admin-referral-leaders')).toBeVisible();
});
```

- [ ] **Step 15.3: Передеплой + запуск**

```bash
pnpm build && rsync -az --delete -e "ssh -p 60322" dist/ dvolkov@82.202.197.230:/var/www/spirits/dist/
pnpm exec playwright test tests/e2e/flows/08-admin.spec.ts --project=chromium-admin
```

- [ ] **Step 15.4: Commit**

```bash
git add src/components/admin/ src/pages/AdminPage.tsx tests/e2e/flows/08-admin.spec.ts
git commit -m "test: E2E flow 08 — админка (assistants/coupons/referral)"
```

---

## Task 16: Flow 09 — реферальная регистрация через UI

**Files:**
- Create: `tests/e2e/flows/09-referral.spec.ts`
- Create: `tests/e2e/helpers/referral.ts` — SSH-cleanup функции

- [ ] **Step 16.1: Написать `tests/e2e/helpers/referral.ts`**

```ts
import { execSync } from 'node:child_process';
import { assertIsTestPhone } from './guards';

const SSH_TARGET = 'dvolkov@82.202.197.230';
const SSH_PORT = '60322';

/**
 * Удалить пользователя по телефону из всех связанных таблиц.
 * Выполняется на сервере через psql.
 */
export function deleteUserByPhone(phone: string): void {
  assertIsTestPhone(phone);

  // Удаляем в правильном порядке из-за FK (или каскадом, если ON DELETE CASCADE).
  const sql = [
    `DELETE FROM referral_commissions WHERE referee_phone='${phone}';`,
    `DELETE FROM referral_registrations WHERE referee_phone='${phone}';`,
    `DELETE FROM users WHERE phone='${phone}';`,
  ].join(' ');

  execSync(
    `ssh -p ${SSH_PORT} ${SSH_TARGET} "psql linkeon -c \\"${sql}\\""`,
    { stdio: 'pipe' },
  );
}

/**
 * Проверить, что referral_registrations содержит связь referrer → referee.
 */
export function checkReferralRegistration(
  referrerPhone: string,
  refereePhone: string,
): boolean {
  assertIsTestPhone(referrerPhone);
  assertIsTestPhone(refereePhone);

  const out = execSync(
    `ssh -p ${SSH_PORT} ${SSH_TARGET} "psql linkeon -t -c \\"SELECT COUNT(*) FROM referral_registrations WHERE referrer_phone='${referrerPhone}' AND referee_phone='${refereePhone}';\\""`,
    { encoding: 'utf-8' },
  );
  return parseInt(out.trim(), 10) > 0;
}

/**
 * Получить slug лидера по телефону.
 */
export function getLeaderSlug(phone: string): string {
  assertIsTestPhone(phone);
  const out = execSync(
    `ssh -p ${SSH_PORT} ${SSH_TARGET} "psql linkeon -t -c \\"SELECT slug FROM referral_leaders WHERE phone='${phone}' AND active=true LIMIT 1;\\""`,
    { encoding: 'utf-8' },
  );
  const slug = out.trim();
  if (!slug) throw new Error(`No active referral slug for ${phone}`);
  return slug;
}
```

> **Важно:** имена таблиц и колонок (`referral_commissions`, `referral_registrations`, `referral_leaders`, `referee_phone`, `referrer_phone`, `slug`, `active`) взяты по смыслу из spec `~/Downloads/spirits_back/tests/referral.e2e.sh`. **Перед реализацией свериться с этим скриптом** и подставить фактические имена.

- [ ] **Step 16.2: Написать `09-referral.spec.ts`**

```ts
import { test, expect } from '@playwright/test';
import { fetchOtp } from '../helpers/otp';
import { generateTempReferralPhone } from '../helpers/guards';
import {
  deleteUserByPhone,
  checkReferralRegistration,
  getLeaderSlug,
} from '../helpers/referral';
import { TEST_PHONES, BASE_URL, TIMEOUTS } from '../helpers/testData';

// Anon — онбординг с чистого листа.
test.use({ storageState: { cookies: [], origins: [] } });

test('09 referral: регистрация по ссылке ?ref=', async ({ page }) => {
  const slug = getLeaderSlug(TEST_PHONES.ADMIN);
  const refereePhone = generateTempReferralPhone();

  try {
    // Открываем с ref-параметром.
    await page.goto(`${BASE_URL}/?ref=${slug}`);
    await expect(page.getByTestId('onboarding-root')).toBeVisible();

    // Проходим онбординг (копия логики из flow 01, но с динамическим номером).
    await page.getByTestId('phone-input').fill(refereePhone);
    await Promise.all([
      page.waitForResponse(r => r.url().includes('/sms/') && r.status() < 400),
      page.getByTestId('phone-submit-btn').click(),
    ]);

    const code = await fetchOtp(refereePhone, { retries: 10, delayMs: 1000 });

    const singleOtp = page.getByTestId('otp-input');
    if (await singleOtp.count()) {
      await singleOtp.fill(code);
    } else {
      for (let i = 0; i < code.length; i++) {
        await page.getByTestId(`otp-input-${i}`).fill(code[i]);
      }
    }

    const otpSubmit = page.getByTestId('otp-submit-btn');
    if (await otpSubmit.count()) await otpSubmit.click();

    const legalBtn = page.getByTestId('legal-accept-btn');
    if (await legalBtn.count() && await legalBtn.isVisible().catch(() => false)) {
      await legalBtn.click();
    }

    await page.waitForURL(/\/chat/, { timeout: TIMEOUTS.STREAM });

    // SQL-проверка: связь создана.
    expect(checkReferralRegistration(TEST_PHONES.ADMIN, refereePhone)).toBe(true);
  } finally {
    deleteUserByPhone(refereePhone);
  }
});
```

- [ ] **Step 16.3: Запустить**

```bash
pnpm exec playwright test tests/e2e/flows/09-referral.spec.ts --project=chromium-anon
```

> **Caveat:** для работы SSH-хелперов нужно настроенное подключение `ssh -p 60322 dvolkov@82.202.197.230` без пароля (SSH-агент / ключи). Если подключение по паролю — тест нужно запускать в окружении, где ключ доступен.

- [ ] **Step 16.4: Commit**

```bash
git add tests/e2e/helpers/referral.ts tests/e2e/flows/09-referral.spec.ts
git commit -m "test: E2E flow 09 — регистрация по ref-ссылке + SQL-верификация"
```

---

## Task 17: Flow 10 — мобильный layout

**Files:**
- Modify: `src/components/layout/Navigation.tsx` — `data-testid`
- Create: `tests/e2e/flows/10-mobile-layout.spec.ts`

- [ ] **Step 17.1: Добавить data-testid в Navigation.tsx**

- Корень мобильной bottom-nav: `data-testid="mobile-bottom-nav"` (только на мобильной версии, по Tailwind media query)
- Каждая кнопка навигации: `data-testid="nav-chat"`, `"nav-profile"`, `"nav-search"`, `"nav-settings"`, `"nav-compatibility"` и т.д.

- [ ] **Step 17.2: Написать `10-mobile-layout.spec.ts`**

```ts
import { test, expect } from '@playwright/test';
import { TIMEOUTS } from '../helpers/testData';

// Используем mobile-user project (iPhone 13 viewport).
test.describe('10 mobile layout', () => {
  test('bottom nav и переходы между вкладками', async ({ page }) => {
    await page.goto('/chat');
    await expect(page.getByTestId('mobile-bottom-nav')).toBeVisible({ timeout: TIMEOUTS.API });
    await expect(page.getByTestId('chat-root')).toBeVisible();

    await page.getByTestId('nav-profile').click();
    await page.waitForURL(/\/profile/);
    await expect(page.getByTestId('profile-root')).toBeVisible();

    await page.getByTestId('nav-search').click();
    await page.waitForURL(/\/search/);
    await expect(page.getByTestId('search-root')).toBeVisible();

    await page.getByTestId('nav-chat').click();
    await page.waitForURL(/\/chat/);
    await expect(page.getByTestId('chat-root')).toBeVisible();
  });
});
```

- [ ] **Step 17.3: Передеплой + запуск на mobile-user project**

```bash
pnpm build && rsync -az --delete -e "ssh -p 60322" dist/ dvolkov@82.202.197.230:/var/www/spirits/dist/
pnpm exec playwright test tests/e2e/flows/10-mobile-layout.spec.ts --project=mobile-user
```

- [ ] **Step 17.4: Commit**

```bash
git add src/components/layout/Navigation.tsx tests/e2e/flows/10-mobile-layout.spec.ts
git commit -m "test: E2E flow 10 — mobile bottom-nav, переходы между вкладками"
```

---

## Task 18: Полный прогон + README + CLAUDE.md

**Files:**
- Create: `tests/e2e/README.md`
- Modify: `CLAUDE.md`

- [ ] **Step 18.1: Полный прогон всех flows**

```bash
pnpm exec playwright test
```

Expected: все 10 flows + helper-тесты проходят.

Если что-то падает — разобраться (обычно flakey из-за таймаутов стрима, фикс — увеличить `TIMEOUTS.STREAM` или добавить `expect.toPass({ intervals: [...] })`).

- [ ] **Step 18.2: Повторный прогон (проверка идемпотентности)**

```bash
pnpm exec playwright test
```

Второй прогон подряд без ручного cleanup — должен проходить так же, как первый. Если какой-то flow падает во второй раз — его cleanup неполный, доработать.

- [ ] **Step 18.3: Написать `tests/e2e/README.md`**

```markdown
# E2E-тесты my.linkeon.io

## Требования

- `pnpm install` уже выполнен
- `pnpm exec playwright install chromium webkit` выполнен
- Сервер `82.202.197.230:60322` доступен по SSH с ключом (для flow 09 — referral)
- На бэке `DEBUG_SMS_CODES=true` (для получения OTP-кодов)

## Запуск

```bash
pnpm test:e2e              # все flows
pnpm test:e2e:ui           # Playwright UI mode (лучший режим для отладки)
pnpm test:e2e:headed       # видимый браузер
pnpm test:e2e:debug        # step-by-step дебаг (PWDEBUG=1)

# Конкретный flow
pnpm exec playwright test tests/e2e/flows/02-chat.spec.ts

# Конкретный project
pnpm exec playwright test --project=chromium-admin
```

## Flows

| # | Файл | Проект | Что проверяет |
|---|------|--------|---------------|
| 01 | onboarding | chromium-anon | Телефон → SMS → OTP → /chat |
| 02 | chat | chromium-user | Сообщение → стриминг → списание токенов |
| 03 | assistant-switch | chromium-user | Изоляция истории между ассистентами |
| 04 | profile | chromium-user | Edit firstName → reload → persisted |
| 05 | tokens-purchase | chromium-user | Mock YooKassa → /payment/success → balance +50k |
| 06 | coupon | chromium-user | Admin создаёт купон → user активирует → balance |
| 07 | search-compatibility | chromium-user | Поиск → UserProfileModal → совместимость |
| 08 | admin | chromium-admin | Три вкладки админки, у всех непустые данные |
| 09 | referral | chromium-anon | ?ref={slug} → регистрация → SQL-проверка |
| 10 | mobile-layout | mobile-user | Bottom nav, переходы между табами |

## Тестовые аккаунты

- USER: `70000000000`
- ADMIN: `79030169187` (также реферальный лидер)

Оба — только для автотестов, в продакшене не использовать для реальных действий.

## Сейф-гард

Все helpers проверяют телефон через `assertIsTestPhone` → бросают исключение,
если тест пытается использовать произвольный номер. Бэкенд дублирует проверку.

## Troubleshooting

- `fetchOtp: no code` — SMS не запрашивался или `DEBUG_SMS_CODES != 'true'` на бэке.
- `globalSetup: saved auth state ... failed` — проверить что webhook UUID SMS не поменялся (см. `helpers/login.ts`).
- `data-testid` не находится — забыли передеплой фронта на prod. Выполнить `pnpm build && rsync`.
- Flow 09 падает на SSH — ключи не настроены, запустить `ssh -p 60322 dvolkov@82.202.197.230 echo ok` для проверки.
```

- [ ] **Step 18.4: Обновить `CLAUDE.md` фронта**

Добавить в конец секцию:

```markdown
## Автотесты фронта

Playwright E2E в `tests/e2e/` — 10 flows, покрывают критические user-пути на prod.

```bash
pnpm test:e2e              # все
pnpm test:e2e:ui           # UI mode для отладки
```

Перед запуском тестов обычно нужен `pnpm build && rsync dist/ на сервер`, чтобы свежие `data-testid` попали на prod.

Подробнее: [tests/e2e/README.md](tests/e2e/README.md).
```

- [ ] **Step 18.5: Commit**

```bash
git add tests/e2e/README.md CLAUDE.md
git commit -m "docs: README для E2E-тестов + ссылка в CLAUDE.md"
```

- [ ] **Step 18.6: Push**

```bash
git push origin HEAD
```

---

## Критерии готовности

- [ ] `pnpm test:e2e` проходит все 10 flows + guards + otp helpers.
- [ ] Повторный прогон без ручного cleanup тоже проходит.
- [ ] `spirits_back/tests/` (50 тестов) продолжают проходить: `cd ~/Downloads/spirits_back/tests && node runner.js`.
- [ ] `pnpm build` не сломан добавленными `data-testid`.
- [ ] `CLAUDE.md` фронта ссылается на `tests/e2e/README.md`.
- [ ] `.auth/*.json` в `.gitignore`, в репо не попали.
