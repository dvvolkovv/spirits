# Kindred Spirits

Веб-приложение для поиска совместимых людей и общения с AI-ассистентами. Платформа позволяет пользователям создавать профили, общаться с виртуальными ассистентами, находить совместимых пользователей и управлять токенами для доступа к функциям приложения.

## 🚀 Технологии

- **React 18** - UI библиотека
- **TypeScript** - типизированный JavaScript
- **Vite** - быстрый сборщик и dev-сервер
- **React Router** - маршрутизация
- **Tailwind CSS** - утилитарный CSS фреймворк
- **i18next** - интернационализация (русский/английский)
- **Supabase** - база данных и бэкенд сервисы
- **Lucide React** - иконки
- **React Markdown** - рендеринг markdown контента

## 📁 Структура проекта

```
spirits/
├── public/                 # Статические файлы
├── src/
│   ├── components/         # React компоненты
│   │   ├── admin/          # Админ-панель
│   │   ├── chat/           # Компоненты чата
│   │   ├── chats/          # Список чатов
│   │   ├── layout/         # Компоненты макета
│   │   ├── onboarding/    # Онбординг
│   │   ├── profile/        # Профиль пользователя
│   │   ├── search/         # Поиск пользователей
│   │   ├── settings/       # Настройки
│   │   └── tokens/         # Управление токенами
│   ├── contexts/           # React контексты
│   ├── i18n/              # Локализация
│   ├── pages/             # Страницы приложения
│   ├── services/          # API сервисы
│   ├── types/             # TypeScript типы
│   └── utils/             # Утилиты
├── supabase/              # Миграции Supabase
└── vite.config.ts         # Конфигурация Vite
```

## 📋 Требования

- **Node.js** версии 18 или выше
- **npm** или **pnpm** (рекомендуется pnpm)
- Аккаунт и доступ к бэкенд API

## 🔧 Установка

1. Клонируйте репозиторий:
```bash
git clone <repository-url>
cd spirits
```

2. Установите зависимости:
```bash
# С npm
npm install

# Или с pnpm (рекомендуется)
pnpm install
```

## ⚙️ Настройка переменных окружения

Создайте файл `.env` в корне проекта со следующими переменными:

```env
# URL бэкенд API
VITE_BACKEND_URL=https://travel-n8n.up.railway.app

# Режим обслуживания (опционально)
VITE_MAINTENANCE_MODE=false

# Supabase конфигурация (если используется)
VITE_SUPABASE_URL=your_supabase_url
VITE_SUPABASE_ANON_KEY=your_supabase_anon_key
```

> **Примечание:** Если переменные окружения не заданы, приложение будет использовать значения по умолчанию из кода.

## 🏃 Запуск локальной версии для отладки

### Режим разработки

Запустите dev-сервер с hot-reload:

```bash
# С npm
npm run dev

# Или с pnpm
pnpm dev
```

Приложение будет доступно по адресу: **http://localhost:5173**

Vite автоматически откроет браузер. Изменения в коде будут применяться автоматически без перезагрузки страницы.

### Дополнительные команды

```bash
# Сборка для продакшена
npm run build
# или
pnpm build

# Предпросмотр production сборки
npm run preview
# или
pnpm preview

# Проверка кода линтером
npm run lint
# или
pnpm lint
```

## 🛠️ Отладка

### DevTools

1. Откройте DevTools браузера (F12)
2. Используйте вкладку **Console** для просмотра логов
3. Вкладка **Network** для мониторинга API запросов
4. Вкладка **Application** для просмотра localStorage и IndexedDB

### Типичные проблемы

**Проблема:** Приложение не подключается к API
- **Решение:** Проверьте переменную `VITE_BACKEND_URL` в `.env` файле

**Проблема:** Ошибки авторизации
- **Решение:** Убедитесь, что токены доступа корректны и не истекли

**Проблема:** Стили не применяются
- **Решение:** Убедитесь, что Tailwind CSS правильно настроен и файлы пересобраны

### Кеш аватаров

Приложение использует IndexedDB для кеширования аватаров. Для очистки кеша:
1. Откройте DevTools → Application → IndexedDB
2. Найдите базу данных `KindredSpiritsCache`
3. Удалите хранилище `avatars`

Или используйте функцию очистки в коде:
```typescript
import { avatarService } from './services/avatarService';
await avatarService.clearCache();
```

## 📦 Сборка для продакшена

```bash
npm run build
```

Собранные файлы будут в папке `dist/`. Эти файлы можно развернуть на любом статическом хостинге (Vercel, Netlify, GitHub Pages и т.д.).

## 🌐 Основные функции

- **Аутентификация** - вход по номеру телефона с OTP
- **Профиль пользователя** - создание и редактирование профиля
- **Чат с AI-ассистентами** - общение с различными виртуальными ассистентами
- **Поиск совместимости** - поиск пользователей на основе совместимости
- **Управление токенами** - система токенов для доступа к функциям
- **Админ-панель** - управление ассистентами (для администраторов)
- **Интернационализация** - поддержка русского и английского языков

## 📝 Дополнительная документация

- [API Integration Guide](./API_INTEGRATION_GUIDE.md) - руководство по интеграции с API
- [YooKassa Integration](./YOOKASSA_INTEGRATION.md) - интеграция платежной системы

## 🤝 Разработка

### Добавление новых компонентов

1. Создайте компонент в соответствующей папке `src/components/`
2. Используйте TypeScript для типизации
3. Следуйте существующим паттернам стилизации с Tailwind CSS
4. Добавьте локализацию в `src/i18n/locales/`

### Работа с API

Все API запросы выполняются через `apiClient` из `src/services/apiClient.ts`. Клиент автоматически:
- Добавляет токены авторизации
- Обрабатывает обновление токенов
- Управляет повторными запросами при ошибках 401

## 📄 Лицензия

Проект является приватным.

## 👥 Контакты

Для вопросов и поддержки обращайтесь к команде разработки.
